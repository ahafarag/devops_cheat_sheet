window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Core',
  number:'TAB 1',
  title:'Java Core / Language Fundamentals',
  color:'var(--accent1)',
  what:'The foundation every Java engineer must own cold — <em>types, OOP mechanics, strings, exceptions, and the equals/hashCode contract</em>. Interviewers probe here to test whether you actually understand the language vs. just use it.',
  topics:[
    {name:'Primitives, Wrappers & Autoboxing',tag:'basic',body:'<div class="explanation">Java has <strong>8 primitives</strong>: <code>byte</code>(8), <code>short</code>(16), <code>int</code>(32), <code>long</code>(64), <code>float</code>(32), <code>double</code>(64), <code>char</code>(16, unsigned), <code>boolean</code>. Each has a wrapper class (<code>Integer</code>, <code>Long</code>...). <strong>Autoboxing</strong> converts primitive↔wrapper automatically.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>int a = 5;\nInteger boxed = a;          // autoboxing: int -&gt; Integer\nint b = boxed;              // unboxing: Integer -&gt; int\n\n// Integer cache: -128..127 are cached &amp; reused\nInteger x = 127, y = 127;\nSystem.out.println(x == y); // true  (same cached object)\nInteger p = 128, q = 128;\nSystem.out.println(p == q); // false (different objects!)\nSystem.out.println(p.equals(q)); // true (always compare with equals)</pre></div><div class="gotcha"><div class="gotcha-label">Gotcha</div>Use <code>==</code> on primitives, <code>.equals()</code> on wrappers/objects. <code>==</code> on wrappers compares references, not values. Also: unboxing a <code>null</code> Integer throws <code>NullPointerException</code>.</div>'},
    {name:'Strings — Immutability, Pool & StringBuilder',tag:'basic',body:'<div class="explanation"><code>String</code> is <strong>immutable</strong> — every modification creates a new object. String literals live in the <strong>String pool</strong> (interned) for reuse. For heavy concatenation in loops use <code>StringBuilder</code> (mutable, not thread-safe) or <code>StringBuffer</code> (synchronized).</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>String a = "hello";          // goes to string pool\nString b = "hello";          // reuses pooled instance\nSystem.out.println(a == b);  // true (same pooled ref)\n\nString c = new String("hello"); // forces NEW heap object\nSystem.out.println(a == c);  // false\nSystem.out.println(a.equals(c)); // true\n\n// BAD: creates N intermediate strings (O(n^2))\nString s = "";\nfor (int i = 0; i &lt; 1000; i++) s += i;\n\n// GOOD: O(n)\nStringBuilder sb = new StringBuilder();\nfor (int i = 0; i &lt; 1000; i++) sb.append(i);\nString result = sb.toString();</pre></div><div class="phrase"><div class="phrase-label">Why immutable matters</div>Thread-safety (shareable without locks), safe for hash keys, security (paths/URLs can\'t be mutated after validation), and pooling/caching. The hashCode is cached after first computation.</div>'},
    {name:'OOP Basics — Classes, Objects, Constructors',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>public class Account {\n    private final String id;       // field\n    private double balance;\n\n    public Account(String id) {    // constructor\n        this.id = id;\n        this.balance = 0.0;\n    }\n    public Account(String id, double balance) { // overloaded ctor\n        this(id);                  // delegate to other ctor\n        this.balance = balance;\n    }\n    public void deposit(double amt) {\n        if (amt &lt;= 0) throw new IllegalArgumentException("amt &gt; 0");\n        this.balance += amt;\n    }\n    public double getBalance() { return balance; }\n}\n\nAccount acc = new Account("A1", 100.0);\nacc.deposit(50);</pre></div><div class="explanation"><strong>Access modifiers:</strong> <code>private</code> (class only), default/package-private (no keyword), <code>protected</code> (package + subclasses), <code>public</code> (everyone). Default to the most restrictive that works — encapsulation.</div>'},
    {name:'Inheritance, Polymorphism & Method Overriding',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>abstract class Shape {\n    abstract double area();            // must be implemented\n    String describe() { return "Shape area=" + area(); }\n}\nclass Circle extends Shape {\n    private final double r;\n    Circle(double r) { this.r = r; }\n    @Override double area() { return Math.PI * r * r; }\n}\n\nShape s = new Circle(2);   // upcasting\nSystem.out.println(s.area());      // 12.56 (dynamic dispatch)\nSystem.out.println(s.describe());  // calls overridden area()</pre></div><div class="explanation"><strong>Overriding</strong> (runtime polymorphism): same signature in subclass, resolved by actual object type via the virtual method table. <strong>Overloading</strong> (compile-time): same name, different params, resolved by reference type. Use <code>@Override</code> always — it catches signature mistakes.</div><div class="gotcha"><div class="gotcha-label">Rules</div><code>final</code> methods/classes can\'t be overridden/extended. <code>static</code> methods are <em>hidden</em>, not overridden. Overriding method can\'t reduce visibility or throw broader checked exceptions.</div>'},
    {name:'Abstract Classes vs Interfaces',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>interface Payable {\n    void pay(double amt);                 // abstract\n    default void payTwice(double amt) {    // default method (Java 8+)\n        pay(amt); pay(amt);\n    }\n    static Payable noop() { return amt -&gt; {}; } // static method\n}\n\nabstract class BaseService {\n    protected String name;                 // can hold state\n    BaseService(String n) { this.name = n; }\n    abstract void run();                    // subclass implements\n    void log() { System.out.println(name); }// concrete method\n}</pre></div><div class="q-block"><div class="q-label">Classic Question</div><div class="q-text">Abstract class vs interface — when do you use each?</div><div class="q-answer"><strong>Interface:</strong> defines a capability/contract, supports multiple inheritance of type, no instance state (only constants), all methods public. Use for "can-do" relationships and decoupling. <strong>Abstract class:</strong> shares common state + partial implementation among related classes, single inheritance, can have any access modifier and constructors. Use for "is-a" with shared code. Since Java 8 interfaces have <code>default</code>/<code>static</code> methods, blurring the line — but interfaces still can\'t hold mutable state.</div></div>'},
    {name:'Exception Handling — Checked vs Unchecked',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// try-with-resources auto-closes (AutoCloseable)\ntry (var reader = Files.newBufferedReader(path)) {\n    return reader.readLine();\n} catch (NoSuchFileException e) {      // specific first\n    throw new ConfigException("missing config", e); // wrap, keep cause\n} catch (IOException e) {              // broader after\n    log.error("read failed", e);\n    throw new UncheckedIOException(e);\n} finally {\n    // always runs (cleanup) — avoid returning here\n}</pre></div><div class="explanation"><strong>Checked</strong> (extends <code>Exception</code>): compiler forces handle-or-declare — recoverable conditions (IO, parsing). <strong>Unchecked</strong> (extends <code>RuntimeException</code>): programming errors (<code>NullPointerException</code>, <code>IllegalArgumentException</code>). <strong>Error</strong>: JVM-level, don\'t catch (<code>OutOfMemoryError</code>).</div><div class="gotcha"><div class="gotcha-label">Best practices</div>Catch specific before general. Never swallow exceptions (empty catch). Preserve the cause when wrapping (<code>new X(msg, e)</code>). Don\'t use exceptions for control flow. Prefer try-with-resources over manual finally-close.</div>'},
    {name:'The equals() & hashCode() Contract',tag:'inter',body:'<div class="explanation">If you override one, override both. The contract: equal objects MUST have equal hash codes (the reverse need not hold). Violating it breaks <code>HashMap</code>/<code>HashSet</code> — objects get lost in buckets.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>public final class Point {\n    private final int x, y;\n    public Point(int x, int y) { this.x = x; this.y = y; }\n\n    @Override public boolean equals(Object o) {\n        if (this == o) return true;\n        if (!(o instanceof Point p)) return false; // pattern matching\n        return x == p.x &amp;&amp; y == p.y;\n    }\n    @Override public int hashCode() {\n        return Objects.hash(x, y);   // null-safe, combines fields\n    }\n}</pre></div><div class="phrase"><div class="phrase-label">Say this</div>The five equals properties: reflexive, symmetric, transitive, consistent, and non-null. In modern Java I\'d use a <code>record</code> — it generates a correct equals/hashCode/toString automatically.</div>'},
    {name:'Pass-by-Value (always)',tag:'inter',body:'<div class="explanation">Java is <strong>always pass-by-value</strong>. For objects, the <em>value of the reference</em> is copied — so you can mutate the object the reference points to, but reassigning the parameter does NOT affect the caller.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>void mutate(StringBuilder sb) { sb.append("!"); }  // affects caller\nvoid reassign(StringBuilder sb) { sb = new StringBuilder("new"); } // does NOT\n\nStringBuilder s = new StringBuilder("hi");\nmutate(s);    System.out.println(s); // hi!\nreassign(s);  System.out.println(s); // hi!  (unchanged)</pre></div><div class="gotcha"><div class="gotcha-label">Interview trap</div>Never say "objects are passed by reference." Say: "the reference is passed by value." This distinction is a common senior-level filter.</div>'}
  ]
});
