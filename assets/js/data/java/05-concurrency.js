window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Concurrency',
  number:'TAB 5',
  title:'Concurrency & Multithreading',
  color:'var(--accent5)',
  what:'Where senior Java interviews get hard. Know the <em>thread lifecycle, the memory model (happens-before), synchronized vs volatile vs atomics, the Executor framework, CompletableFuture, and the classic bugs</em>: races, deadlocks, visibility.',
  topics:[
    {name:'Threads & the Lifecycle',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">STATES</span></div><pre>NEW -&gt; RUNNABLE -&gt; (RUNNING) -&gt; TERMINATED\n          |  ^\n          v  |\n   BLOCKED / WAITING / TIMED_WAITING\n\nNEW            created, not started\nRUNNABLE       eligible to run (may be running or waiting for CPU)\nBLOCKED        waiting for a monitor lock\nWAITING        wait(), join() with no timeout\nTIMED_WAITING  sleep(ms), wait(ms)\nTERMINATED     run() finished</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA — prefer Runnable</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>Runnable task = () -&gt; System.out.println(Thread.currentThread().getName());\nThread t = new Thread(task);\nt.start();          // start() spawns a thread; run() would NOT\nt.join();           // wait for it to finish</pre></div>'},
    {name:'synchronized vs volatile vs Atomics',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">WHEN TO USE WHAT</span></div><pre>synchronized   Mutual exclusion + visibility. Guards compound\n               (read-modify-write) operations. Reentrant.\n\nvolatile       Visibility ONLY (no atomicity). Reads/writes go to\n               main memory. Good for flags. count++ is NOT safe.\n\nAtomic*        Lock-free atomic operations via CAS.\n               AtomicInteger.incrementAndGet() IS safe &amp; fast.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>private volatile boolean running = true;   // visibility flag\npublic void stop() { running = false; }    // other threads see it\n\nprivate final AtomicInteger count = new AtomicInteger();\ncount.incrementAndGet();                    // atomic, no lock\n\nprivate final Object lock = new Object();\nsynchronized (lock) { balance += amt; }     // compound op needs a lock</pre></div><div class="gotcha"><div class="gotcha-label">The #1 trap</div><code>volatile</code> does NOT make <code>count++</code> thread-safe — that\'s read-modify-write (3 ops). Use <code>AtomicInteger</code> or <code>synchronized</code>.</div>'},
    {name:'Java Memory Model & happens-before',tag:'adv',body:'<div class="explanation">The JMM defines when one thread\'s writes become visible to another. Without a <strong>happens-before</strong> relationship, the JVM/CPU may reorder or cache, and a thread can read stale data.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">HAPPENS-BEFORE EDGES</span></div><pre>- Program order within a single thread\n- Unlock of a monitor -&gt; subsequent lock of the SAME monitor\n- Write to volatile -&gt; subsequent read of that volatile\n- Thread.start() -&gt; first action in the started thread\n- Last action in a thread -&gt; another thread\'s join() returning\n- final fields -&gt; visible after constructor completes (safe publication)</pre></div><div class="phrase"><div class="phrase-label">Say this</div>"Thread-safe" means correct under concurrent access without external synchronization. Most bugs are visibility (stale reads) or atomicity (lost updates) issues — both solved by establishing happens-before via locks, volatile, or atomics.</div>'},
    {name:'Executor Framework & Thread Pools',tag:'inter',body:'<div class="explanation">Never <code>new Thread()</code> per task in production — creation is expensive and unbounded threads exhaust memory. Use an <code>ExecutorService</code> (a managed pool).</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>ExecutorService pool = Executors.newFixedThreadPool(8);\nFuture&lt;Integer&gt; f = pool.submit(() -&gt; compute());\nInteger result = f.get();          // blocks for result\n\npool.shutdown();                    // no new tasks; finish queued\nif (!pool.awaitTermination(30, SECONDS)) pool.shutdownNow();\n\n// Production: tune your own ThreadPoolExecutor (bounded queue!)\nvar exec = new ThreadPoolExecutor(\n    4, 8, 60L, SECONDS,\n    new ArrayBlockingQueue&lt;&gt;(1000),          // bounded — backpressure\n    new ThreadPoolExecutor.CallerRunsPolicy()); // rejection policy</pre></div><div class="gotcha"><div class="gotcha-label">Gotcha</div><code>Executors.newCachedThreadPool()</code> and <code>newFixedThreadPool</code> use an <em>unbounded</em> queue — under load it can OOM. Prefer an explicit <code>ThreadPoolExecutor</code> with a bounded queue and a rejection policy.</div>'},
    {name:'CompletableFuture — async composition',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>CompletableFuture&lt;String&gt; cf = CompletableFuture\n    .supplyAsync(() -&gt; fetchUser(id), pool)      // run async\n    .thenApply(User::email)                       // transform\n    .thenCompose(email -&gt; sendAsync(email))        // chain another future\n    .exceptionally(ex -&gt; { log.error("fail", ex); return "fallback"; });\n\n// combine independent calls in parallel\nvar a = CompletableFuture.supplyAsync(() -&gt; priceFromA());\nvar b = CompletableFuture.supplyAsync(() -&gt; priceFromB());\nint best = a.thenCombine(b, Math::min).join();\n\n// wait for all\nCompletableFuture.allOf(a, b).join();</pre></div><div class="phrase"><div class="phrase-label">Say this</div><code>thenApply</code> is for sync transforms, <code>thenCompose</code> flattens a future-returning call (like flatMap), <code>thenCombine</code> joins two independent futures. Always supply an explicit executor for blocking work — the default common ForkJoinPool is shared and small.</div>'},
    {name:'Concurrency Pitfalls — Deadlock & Races',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">DEADLOCK — two locks, opposite order</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// Thread 1: lock(A) then lock(B)   |  Thread 2: lock(B) then lock(A)\n// -> both wait forever\n\n// FIX: always acquire locks in a consistent global order\nsynchronized (first)  { synchronized (second) { transfer(); } }\n// where first/second are ordered by e.g. account id</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">CHECKLIST</span></div><pre>Race condition   Unsynchronized shared mutable state. Fix: lock/atomic/immutable.\nDeadlock         Circular lock wait. Fix: lock ordering, tryLock + timeout.\nLivelock         Threads react to each other, no progress. Fix: backoff/randomize.\nStarvation       A thread never gets the lock. Fix: fair locks.\nVisibility       Stale reads. Fix: volatile / synchronized / final.\n\nGolden rule: prefer immutability &amp; higher-level concurrency utils\n(java.util.concurrent) over hand-rolled locking.</pre></div>'},
    {name:'Locks & Concurrency Utilities',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>ReentrantLock lock = new ReentrantLock();\nlock.lock();\ntry { /* critical section */ } finally { lock.unlock(); } // ALWAYS unlock in finally\n\nif (lock.tryLock(1, SECONDS)) { ... }   // avoid deadlock with timeout\n\nReadWriteLock rw = new ReentrantReadWriteLock(); // many readers, one writer\n\nCountDownLatch latch = new CountDownLatch(3);  // wait for N events\nSemaphore sem = new Semaphore(10);             // limit concurrent access\nCyclicBarrier barrier = new CyclicBarrier(4);  // threads wait for each other\nBlockingQueue&lt;Task&gt; q = new LinkedBlockingQueue&lt;&gt;(); // producer/consumer</pre></div>'}
  ]
});
