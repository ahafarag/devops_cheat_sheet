window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Persistence',
  number:'TAB 8',
  title:'Persistence & Databases',
  color:'var(--accent8)',
  what:'How Java talks to data: <em>JDBC, JPA/Hibernate entity mapping, relationships, transaction isolation, the N+1 problem, caching</em>, plus the SQL fundamentals every backend engineer is expected to know.',
  topics:[
    {name:'JDBC Fundamentals',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// ALWAYS PreparedStatement (prevents SQL injection) + try-with-resources\nString sql = "SELECT id, name FROM users WHERE status = ?";\ntry (Connection c = dataSource.getConnection();\n     PreparedStatement ps = c.prepareStatement(sql)) {\n    ps.setString(1, status);          // parameter binding, not concatenation\n    try (ResultSet rs = ps.executeQuery()) {\n        List&lt;User&gt; users = new ArrayList&lt;&gt;();\n        while (rs.next())\n            users.add(new User(rs.getLong("id"), rs.getString("name")));\n        return users;\n    }\n}</pre></div><div class="gotcha"><div class="gotcha-label">Security</div>NEVER build SQL with string concatenation of user input — that\'s SQL injection. Always bind parameters with <code>?</code> placeholders. Use a connection pool (HikariCP) — opening connections per request is expensive.</div>'},
    {name:'JPA / Hibernate Entity Mapping',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>@Entity\n@Table(name = "orders")\npublic class Order {\n    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n\n    @Column(nullable = false)\n    private String customerId;\n\n    @Enumerated(EnumType.STRING)      // store enum name, NOT ordinal\n    private OrderStatus status;\n\n    @ManyToOne(fetch = FetchType.LAZY) // lazy by default for ToOne? No — EAGER!\n    @JoinColumn(name = "customer_fk")\n    private Customer customer;\n\n    @OneToMany(mappedBy = "order", cascade = CascadeType.ALL, orphanRemoval = true)\n    private List&lt;LineItem&gt; items = new ArrayList&lt;&gt;();\n}</pre></div><div class="gotcha"><div class="gotcha-label">Mapping traps</div>Use <code>EnumType.STRING</code> — ORDINAL breaks if you reorder the enum. <code>@ManyToOne</code>/<code>@OneToOne</code> are EAGER by default — set them LAZY to avoid loading the world. Always model both sides of a bidirectional relationship and keep them in sync.</div>'},
    {name:'The N+1 Query Problem',tag:'adv',body:'<div class="explanation">The most common JPA performance bug: loading N parent rows, then firing one extra query per parent to load its children — 1 + N queries instead of 1 or 2.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">FIX with a fetch join</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// BAD: triggers N+1 when accessing o.getItems()\nList&lt;Order&gt; orders = repo.findAll();\nfor (Order o : orders) o.getItems().size();   // 1 + N queries\n\n// GOOD: single query with join fetch\n@Query("select distinct o from Order o left join fetch o.items")\nList&lt;Order&gt; findAllWithItems();\n\n// or @EntityGraph\n@EntityGraph(attributePaths = "items")\nList&lt;Order&gt; findAll();</pre></div><div class="phrase"><div class="phrase-label">Say this</div>I detect N+1 by enabling <code>spring.jpa.show-sql</code> or Hibernate statistics in tests. Fixes: fetch joins, <code>@EntityGraph</code>, batch fetching (<code>@BatchSize</code>), or a projection/DTO query that selects exactly what the view needs.</div>'},
    {name:'Transactions & Isolation Levels',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">ACID + ISOLATION</span></div><pre>ACID: Atomicity, Consistency, Isolation, Durability\n\nIsolation level      Dirty read  Non-repeatable  Phantom\nREAD_UNCOMMITTED        yes          yes            yes\nREAD_COMMITTED          no           yes            yes   (common default)\nREPEATABLE_READ         no           no             yes   (MySQL default)\nSERIALIZABLE            no           no             no    (slowest, safest)\n\nDirty read       reading another tx\'s uncommitted change\nNon-repeatable   same row read twice gives different values\nPhantom          same query returns new rows on re-read</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">PROPAGATION</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>@Transactional(propagation = Propagation.REQUIRED)   // default: join or create\n@Transactional(propagation = Propagation.REQUIRES_NEW) // always new, suspend outer\n@Transactional(isolation = Isolation.REPEATABLE_READ)</pre></div>'},
    {name:'Caching & Connection Pooling',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">HIBERNATE CACHE LEVELS</span></div><pre>L1 (first-level)   per-Session/EntityManager. ON by default.\n                   Same entity fetched twice in one tx = one DB hit.\nL2 (second-level)  shared across sessions. Opt-in (@Cacheable + Ehcache/etc).\nQuery cache        caches query result IDs. Use carefully.\n\nApp-level: Spring @Cacheable on expensive read methods.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>@Cacheable(value = "products", key = "#id")\npublic Product find(Long id) { return repo.findById(id).orElseThrow(); }\n\n@CacheEvict(value = "products", key = "#p.id")\npublic void update(Product p) { repo.save(p); }</pre></div><div class="phrase"><div class="phrase-label">HikariCP</div>The default Spring Boot pool. Key knobs: <code>maximum-pool-size</code> (start small — often 10), <code>connection-timeout</code>, <code>max-lifetime</code>. More connections is not always faster; size to your DB\'s capacity.</div>'}
  ]
});
