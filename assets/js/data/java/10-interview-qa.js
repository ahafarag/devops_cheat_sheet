window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Interview Q&A',
  number:'TAB 10',
  title:'Interview Q&A',
  color:'var(--accent1)',
  what:'High-frequency questions grouped by topic, with answers framed the way an interviewer wants to hear them. Open a section, cover the question, then check your answer against the model response.',
  topics:[
    {name:'Core Java Q&A',tag:'basic',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Is Java pass-by-value or pass-by-reference?</div><div class="q-answer">Always <strong>pass-by-value</strong>. For objects, the value of the <em>reference</em> is copied — you can mutate the pointed-to object, but reassigning the parameter doesn\'t affect the caller.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Why are Strings immutable?</div><div class="q-answer">Thread-safety without locks, safe use as hash-map keys (cached hashCode), string-pool sharing/memory savings, and security (a validated path/URL can\'t be changed afterward).</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">== vs equals()?</div><div class="q-answer"><code>==</code> compares references (or values for primitives); <code>equals()</code> compares logical equality as defined by the class. Always use <code>equals()</code> for objects, especially wrappers and Strings.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">final vs finally vs finalize?</div><div class="q-answer"><code>final</code>: constant/non-overridable/non-extendable. <code>finally</code>: block that always runs after try/catch. <code>finalize()</code>: deprecated GC callback — never rely on it; use try-with-resources / <code>Cleaner</code>.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Checked vs unchecked exceptions?</div><div class="q-answer">Checked extend <code>Exception</code> — compiler enforces handle-or-declare, for recoverable conditions. Unchecked extend <code>RuntimeException</code> — programming errors. Errors are JVM-level and shouldn\'t be caught.</div></div>'},
    {name:'OOP & Design Q&A',tag:'inter',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Abstract class vs interface?</div><div class="q-answer">Interface = capability/contract, multiple inheritance of type, no mutable state. Abstract class = shared state + partial implementation, single inheritance, can have constructors. Since Java 8 interfaces have default methods but still no instance fields.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Overloading vs overriding?</div><div class="q-answer">Overloading = same name, different parameters, resolved at <strong>compile time</strong> by reference type. Overriding = same signature in a subclass, resolved at <strong>runtime</strong> by actual object type (dynamic dispatch).</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Why favor composition over inheritance?</div><div class="q-answer">Inheritance couples subclass to superclass internals and breaks encapsulation; behavior is fixed at compile time. Composition is flexible, testable, and lets you swap behavior at runtime (Strategy). Inherit only from classes designed for it.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Explain SOLID briefly.</div><div class="q-answer">Single responsibility, Open/closed (extend don\'t modify), Liskov substitution (subtypes honor base contract), Interface segregation (small focused interfaces), Dependency inversion (depend on abstractions). Together they reduce coupling and ease change.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you make a class immutable?</div><div class="q-answer"><code>final</code> class, all fields <code>private final</code>, no setters, initialize in constructor, defensively copy mutable inputs and outputs. Or just use a <code>record</code>.</div></div>'},
    {name:'Collections Q&A',tag:'inter',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">ArrayList vs LinkedList?</div><div class="q-answer">ArrayList = dynamic array, O(1) random access, cache-friendly — the default. LinkedList = doubly-linked nodes, O(1) head/tail insert but O(n) access and high memory. Use ArrayDeque over LinkedList for queues/stacks.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How does HashMap work internally?</div><div class="q-answer">Array of buckets; key\'s hashCode is spread and mapped to an index. Collisions chain in a linked list, converting to a balanced tree after 8 entries (Java 8+). Resizes (doubles) at 0.75 load factor. Average O(1) get/put.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What happens if you use a mutable object as a HashMap key?</div><div class="q-answer">If its hashCode/equals change after insertion, it lands in the wrong bucket and becomes unretrievable. Keys must be effectively immutable.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">HashMap vs ConcurrentHashMap vs Hashtable?</div><div class="q-answer">HashMap: not thread-safe, allows null. Hashtable: fully synchronized (slow), legacy. ConcurrentHashMap: thread-safe via fine-grained locking/CAS, high concurrency, no null keys/values — the modern concurrent choice.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">fail-fast vs fail-safe iterators?</div><div class="q-answer">Fail-fast (ArrayList, HashMap) throw <code>ConcurrentModificationException</code> if structurally modified during iteration. Fail-safe (CopyOnWriteArrayList, ConcurrentHashMap) iterate over a snapshot and don\'t throw.</div></div>'},
    {name:'Modern Java Q&A',tag:'inter',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Intermediate vs terminal stream operations?</div><div class="q-answer">Intermediate (filter, map, sorted) are <strong>lazy</strong> and return a stream; terminal (collect, reduce, forEach, count) <strong>trigger</strong> execution and produce a result/side effect. A stream is consumed after its terminal op.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">When should you use Optional?</div><div class="q-answer">As a return type for values that may be absent. Not for fields, parameters, or collections. Never call <code>.get()</code> unguarded — use <code>map/orElse/orElseThrow</code>.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What problem do records solve?</div><div class="q-answer">Boilerplate for immutable data carriers — they auto-generate the canonical constructor, accessors, equals, hashCode, and toString. Ideal for DTOs and value objects.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What are virtual threads and when do they help?</div><div class="q-answer">JVM-scheduled lightweight threads (Java 21). A blocked virtual thread unmounts its OS carrier, so you can run millions and keep the simple blocking thread-per-request model — great for I/O-bound work. CPU-bound work still wants platform threads sized to cores.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">map vs flatMap?</div><div class="q-answer"><code>map</code> transforms each element 1:1. <code>flatMap</code> transforms each element into a stream and flattens them into one — use it to unwrap nested structures (e.g. <code>Stream&lt;List&lt;T&gt;&gt;</code> → <code>Stream&lt;T&gt;</code>).</div></div>'},
    {name:'Concurrency Q&A',tag:'adv',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">volatile vs synchronized?</div><div class="q-answer"><code>volatile</code> guarantees visibility only (no atomicity), good for flags. <code>synchronized</code> gives both mutual exclusion and visibility, needed for compound read-modify-write operations. <code>volatile count++</code> is still a race.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What is a deadlock and how do you prevent it?</div><div class="q-answer">Two+ threads each hold a lock the other needs, waiting forever. Prevent via consistent global lock ordering, <code>tryLock</code> with timeout, lock-free structures, or reducing lock scope. Detect with a thread dump (jstack).</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Why use a thread pool instead of new Thread()?</div><div class="q-answer">Thread creation is expensive and unbounded threads exhaust memory. A pool reuses threads, bounds concurrency, queues work, and gives lifecycle control. Use a bounded queue + rejection policy for backpressure.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">start() vs run()?</div><div class="q-answer"><code>start()</code> creates a new thread and the JVM calls <code>run()</code> on it. Calling <code>run()</code> directly just executes on the current thread — no concurrency.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What is the happens-before relationship?</div><div class="q-answer">A JMM ordering guarantee that one action\'s effects are visible to another. Established by lock release→acquire, volatile write→read, thread start, and join. Without it, reads may see stale values due to caching/reordering.</div></div>'},
    {name:'JVM & Memory Q&A',tag:'adv',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Stack vs heap memory?</div><div class="q-answer">Stack: per-thread, holds method frames, local primitives and references; freed on method return. Heap: shared, holds all objects/arrays; reclaimed by GC. References live on the stack; the objects they point to live on the heap.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How does garbage collection decide what to collect?</div><div class="q-answer">Reachability from GC roots (threads, static fields, stack vars, JNI). Anything not reachable via a reference chain is collectable. Based on the generational hypothesis: young gen collected frequently/cheaply, old gen less often.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Can you have a memory leak in Java?</div><div class="q-answer">Yes — logical leaks from holding references you no longer need: static/unbounded collections, un-deregistered listeners, ThreadLocals in pooled threads, caches without eviction. GC can\'t collect reachable objects.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How would you diagnose high memory / an OOM?</div><div class="q-answer">Enable <code>-XX:+HeapDumpOnOutOfMemoryError</code>, capture with <code>jmap</code>, analyze in Eclipse MAT (dominator tree, leak suspects, retained size). Use <code>jstat</code> for GC behavior and <code>jstack</code> for threads. Then fix the root retention.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What replaced PermGen and why?</div><div class="q-answer">Metaspace (Java 8). It moved class metadata to native memory that auto-grows, eliminating the fixed-size <code>PermGen space</code> OOMs that plagued app-server redeploys.</div></div>'},
    {name:'Spring Q&A',tag:'inter',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What is dependency injection and why constructor injection?</div><div class="q-answer">DI supplies a class\'s dependencies from outside rather than having it construct them. Constructor injection makes dependencies explicit and mandatory, allows <code>final</code> fields, and lets you instantiate the class in tests without Spring.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Default bean scope? What scopes exist?</div><div class="q-answer">Default is <strong>singleton</strong> (one instance per container). Others: prototype (new per injection), and web scopes request/session/application.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How does @Transactional work and what are its pitfalls?</div><div class="q-answer">It\'s AOP — Spring wraps the bean in a proxy that opens/commits/rolls back a transaction. Pitfalls: self-invocation bypasses the proxy; by default only unchecked exceptions roll back (use <code>rollbackFor</code>); only works on public methods of Spring beans.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What does Spring Boot auto-configuration do?</div><div class="q-answer">Conditionally configures beans based on classpath contents and existing beans (<code>@ConditionalOnClass</code>, <code>@ConditionalOnMissingBean</code>). Starters bundle dependencies; you override any default by defining your own bean.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">@Controller vs @RestController?</div><div class="q-answer"><code>@RestController</code> = <code>@Controller</code> + <code>@ResponseBody</code> — return values are serialized straight to the response body (JSON). <code>@Controller</code> alone resolves view names for server-side rendering.</div></div>'},
    {name:'Persistence Q&A',tag:'adv',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What is the N+1 problem and how do you fix it?</div><div class="q-answer">Loading N parents then 1 query per parent for its children = 1+N queries. Fix with a JPQL <code>join fetch</code>, <code>@EntityGraph</code>, batch fetching, or a DTO projection that selects exactly what\'s needed.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">FetchType LAZY vs EAGER?</div><div class="q-answer">LAZY loads the association on first access (a proxy); EAGER loads it immediately with the parent. <code>@ManyToOne</code>/<code>@OneToOne</code> default EAGER — usually make them LAZY to control queries. Beware LazyInitializationException outside a session.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you prevent SQL injection?</div><div class="q-answer">Use parameterized queries (<code>PreparedStatement</code> with <code>?</code>, or JPA named parameters) — never concatenate user input into SQL. Validate/whitelist where dynamic structure (e.g. sort columns) is needed.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Explain transaction isolation levels.</div><div class="q-answer">READ_UNCOMMITTED (dirty reads) → READ_COMMITTED (no dirty) → REPEATABLE_READ (no non-repeatable) → SERIALIZABLE (no phantoms, fully serial). Higher isolation = more correctness, less concurrency. Pick the lowest that prevents the anomalies you care about.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">First-level vs second-level cache?</div><div class="q-answer">L1 is the per-session persistence context, on by default — repeated lookups in one transaction hit it. L2 is shared across sessions, opt-in via a provider (Ehcache, etc.) and <code>@Cacheable</code>.</div></div>'},
    {name:'Behavioral & System Design Prompts',tag:'inter',body:'<div class="q-block"><div class="q-label">Prompt</div><div class="q-text">Walk me through how a request flows through your Spring Boot service.</div><div class="q-answer">Servlet container (Tomcat) → <code>DispatcherServlet</code> → filters/interceptors → matched <code>@RestController</code> method → request body deserialized + validated → service layer (with <code>@Transactional</code>) → repository/JPA → DB → response serialized to JSON → exception handlers (<code>@RestControllerAdvice</code>) map errors to status codes.</div></div><div class="q-block"><div class="q-label">Prompt</div><div class="q-text">How would you design a rate limiter?</div><div class="q-answer">Token bucket or sliding window. In-memory (<code>Semaphore</code>/Guava RateLimiter) for single node; Redis (<code>INCR</code> + TTL or a Lua sliding window) for distributed. Key by user/IP/API key, return 429 with <code>Retry-After</code> when exceeded.</div></div><div class="q-block"><div class="q-label">Prompt</div><div class="q-text">How do you make a service idempotent?</div><div class="q-answer">Accept a client-supplied idempotency key, store processed keys with their result, and return the stored result on retries. Use DB unique constraints or upserts so concurrent duplicates can\'t double-process.</div></div><div class="q-block"><div class="q-label">Prompt</div><div class="q-text">A production endpoint is slow. How do you investigate?</div><div class="q-answer">Check metrics/APM for where time goes (DB, downstream, GC). Look for N+1 queries, missing indexes, lock contention, or large payloads. Profile with JFR/async-profiler, reproduce under load, fix the dominant cost, then re-measure. Avoid guessing.</div></div><div class="q-block"><div class="q-label">Prompt</div><div class="q-text">How do you keep a codebase maintainable on a team?</div><div class="q-answer">Clear layering and boundaries, small focused classes, constructor injection, meaningful tests (unit + integration with Testcontainers), CI with static analysis and coverage gates, consistent formatting, and code review focused on correctness, security, and readability.</div></div>'}
  ]
});
