window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'OOP & Design',
  number:'TAB 2',
  title:'OOP & Design Principles',
  color:'var(--accent4)',
  what:'Senior interviews care less about syntax and more about <em>design judgment</em> — the four pillars, SOLID, composition over inheritance, and the handful of patterns that show up constantly in real codebases.',
  topics:[
    {name:'The Four Pillars of OOP',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">FOUR PILLARS</span></div><pre>Encapsulation  Hide internal state; expose behavior via methods.\n               private fields + public getters/setters/methods.\n\nInheritance    Reuse + specialize. "is-a" relationship.\n               class Dog extends Animal.\n\nPolymorphism   One interface, many implementations.\n               Runtime (overriding) + compile-time (overloading).\n\nAbstraction    Expose what, hide how.\n               interface / abstract class define the contract.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Encapsulation protects invariants — I never expose mutable internal collections directly; I return copies or unmodifiable views. Abstraction lets callers depend on contracts, not implementations, which is what makes code testable and swappable.</div>'},
    {name:'SOLID Principles',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">SOLID</span></div><pre>S  Single Responsibility  A class has one reason to change.\n   Split a "UserService" that does email + DB + validation.\n\nO  Open/Closed             Open for extension, closed for modification.\n   Add new behavior via new classes (Strategy), not by editing existing.\n\nL  Liskov Substitution     Subtypes must be usable as their base type\n   without breaking expectations. (Square extends Rectangle = classic violation)\n\nI  Interface Segregation    Many small focused interfaces &gt; one fat one.\n   Don\'t force a class to implement methods it doesn\'t need.\n\nD  Dependency Inversion     Depend on abstractions, not concretions.\n   High-level modules and details both depend on interfaces.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">DEPENDENCY INVERSION EXAMPLE</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>interface NotificationSender { void send(String msg); }\n\nclass OrderService {\n    private final NotificationSender sender;     // depend on abstraction\n    OrderService(NotificationSender sender) {    // inject\n        this.sender = sender;\n    }\n    void placeOrder() { /* ... */ sender.send("Order placed"); }\n}\n// Swap EmailSender / SmsSender / a mock in tests without touching OrderService</pre></div>'},
    {name:'Composition over Inheritance',tag:'inter',body:'<div class="explanation">Prefer <strong>has-a</strong> (composition) over <strong>is-a</strong> (inheritance). Inheritance is rigid, couples subclass to superclass internals, and breaks encapsulation. Composition is flexible and testable.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// Inheritance — fragile: subclass depends on Stack internals\n// class History extends ArrayList&lt;String&gt; { ... }  // exposes ALL list methods\n\n// Composition — robust: expose only what you want\nclass History {\n    private final List&lt;String&gt; entries = new ArrayList&lt;&gt;();\n    public void record(String e) { entries.add(e); }\n    public List&lt;String&gt; entries() { return List.copyOf(entries); }\n}</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Effective Java item 18: favor composition. Inheritance is only safe within a package you control or for classes explicitly designed and documented for extension. Otherwise compose and delegate.</div>'},
    {name:'Design Patterns — Creational',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">BUILDER — readable construction of complex objects</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>public class Server {\n    private final String host; private final int port; private final boolean tls;\n    private Server(Builder b) { host=b.host; port=b.port; tls=b.tls; }\n    public static class Builder {\n        private String host = "localhost"; private int port = 8080; private boolean tls;\n        public Builder host(String h){ this.host=h; return this; }\n        public Builder port(int p){ this.port=p; return this; }\n        public Builder tls(boolean t){ this.tls=t; return this; }\n        public Server build(){ return new Server(this); }\n    }\n}\nServer s = new Server.Builder().host("api").port(443).tls(true).build();</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">FACTORY — decouple creation from use</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>interface Parser { Doc parse(String s); }\nclass ParserFactory {\n    static Parser of(String type) {\n        return switch (type) {\n            case "json" -&gt; new JsonParser();\n            case "xml"  -&gt; new XmlParser();\n            default -&gt; throw new IllegalArgumentException(type);\n        };\n    }\n}</pre></div><div class="gotcha"><div class="gotcha-label">Singleton — do it right</div>Prefer an <code>enum</code> singleton (thread-safe, serialization-safe by the JVM) or a Spring-managed singleton bean. Avoid the classic double-checked-locking unless you must — it\'s easy to get wrong.</div>'},
    {name:'Design Patterns — Behavioral & Structural',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">STRATEGY — pluggable algorithms</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>interface DiscountStrategy { double apply(double total); }\n\nMap&lt;String, DiscountStrategy&gt; strategies = Map.of(\n    "none",   t -&gt; t,\n    "black",  t -&gt; t * 0.8,\n    "vip",    t -&gt; t * 0.7\n);\ndouble price = strategies.get(tier).apply(cart.total());</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">QUICK MAP</span></div><pre>Observer    Pub/sub. Subject notifies listeners. (event systems)\nDecorator   Wrap to add behavior. (BufferedReader wraps Reader)\nAdapter     Make incompatible interfaces work together.\nTemplate    Skeleton in base, steps overridden by subclasses.\nProxy       Stand-in controlling access. (Spring AOP, lazy loading)\nChain       Pass request along handlers. (servlet filters)</pre></div><div class="phrase"><div class="phrase-label">Say this</div>I reach for Strategy when I have an <code>if/else</code> or <code>switch</code> on a type that keeps growing — it turns modification into extension (Open/Closed). With lambdas, many GoF patterns collapse to a functional interface.</div>'},
    {name:'Immutability & Value Objects',tag:'inter',body:'<div class="explanation">Immutable objects are inherently thread-safe, cacheable, and safe as map keys. Recipe: <code>final</code> class, all fields <code>private final</code>, no setters, defensive-copy mutable inputs/outputs.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA — pre-records</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>public final class Money {\n    private final long cents; private final String currency;\n    public Money(long cents, String currency) {\n        this.cents = cents; this.currency = Objects.requireNonNull(currency);\n    }\n    public Money plus(Money o) {            // return NEW instance\n        if (!currency.equals(o.currency)) throw new IllegalArgumentException();\n        return new Money(cents + o.cents, currency);\n    }\n}\n\n// Java 16+ records do all of this for you:\npublic record MoneyR(long cents, String currency) {}</pre></div>'}
  ]
});
