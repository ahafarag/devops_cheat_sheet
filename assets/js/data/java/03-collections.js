window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Collections',
  number:'TAB 3',
  title:'Collections & Generics',
  color:'var(--accent3)',
  what:'The most-used part of the standard library and a constant interview topic. Know the <em>hierarchy, the Big-O of each implementation, when to pick which, and how generics + type erasure actually work</em>.',
  topics:[
    {name:'Collection Hierarchy Overview',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">HIERARCHY</span></div><pre>Iterable\n └─ Collection\n     ├─ List   (ordered, indexed, duplicates)   ArrayList, LinkedList\n     ├─ Set    (no duplicates)                  HashSet, LinkedHashSet, TreeSet\n     └─ Queue  (FIFO / priority)                ArrayDeque, PriorityQueue, LinkedList\n\nMap (NOT a Collection — key→value)             HashMap, LinkedHashMap, TreeMap,\n                                               ConcurrentHashMap, Hashtable(legacy)</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Map is not a Collection — it\'s a separate root interface. People conflate them in interviews; calling that out signals you actually know the framework.</div>'},
    {name:'List — ArrayList vs LinkedList',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">BIG-O</span></div><pre>                 ArrayList        LinkedList\nget(i)           O(1)             O(n)\nadd(end)         O(1) amortized   O(1)\nadd/remove(mid)  O(n) (shift)     O(1) at node*  (*O(n) to find node)\nmemory           compact          high (node + 2 pointers each)\n\nVERDICT: Use ArrayList ~95% of the time. It wins on cache locality and\nrandom access. LinkedList only helps for frequent add/remove at the HEAD\n(use ArrayDeque instead — it beats LinkedList for queue/stack too).</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>List&lt;String&gt; list = new ArrayList&lt;&gt;();\nlist.add("a"); list.add("b");\nlist.get(0);                       // O(1)\nList&lt;String&gt; immutable = List.of("x","y");  // immutable (Java 9+)\nlist.removeIf(s -&gt; s.startsWith("a"));      // safe bulk remove</pre></div>'},
    {name:'Set — HashSet, LinkedHashSet, TreeSet',tag:'basic',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CHOOSE</span></div><pre>HashSet        O(1) ops, NO order. Default choice. Needs good hashCode().\nLinkedHashSet  O(1) ops, INSERTION order preserved.\nTreeSet        O(log n) ops, SORTED order. Backed by red-black tree.\n               Needs Comparable or a Comparator. NavigableSet methods\n               (floor, ceiling, headSet, tailSet).</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\nif (!seen.add(x)) System.out.println("duplicate: " + x);\n\nNavigableSet&lt;Integer&gt; ts = new TreeSet&lt;&gt;(List.of(10, 20, 30));\nts.ceiling(15);  // 20 (smallest >= 15)\nts.floor(25);    // 20 (largest <= 25)</pre></div>'},
    {name:'Map — HashMap Internals',tag:'inter',body:'<div class="explanation"><code>HashMap</code> = array of buckets. Key→<code>hashCode()</code>→spread→index. Collisions form a linked list; since Java 8, a bucket with &gt;8 entries converts to a <strong>balanced tree</strong> (O(log n) instead of O(n)). Resizes (doubles) at load factor 0.75.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA — modern Map idioms</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>Map&lt;String, Integer&gt; counts = new HashMap&lt;&gt;();\nfor (String w : words)\n    counts.merge(w, 1, Integer::sum);          // count occurrences\n\nMap&lt;String, List&lt;String&gt;&gt; byFirst = new HashMap&lt;&gt;();\nfor (String name : names)\n    byFirst.computeIfAbsent(name.substring(0,1), k -&gt; new ArrayList&lt;&gt;())\n           .add(name);                          // group\n\ncounts.getOrDefault("x", 0);\ncounts.putIfAbsent("y", 0);</pre></div><div class="gotcha"><div class="gotcha-label">Gotcha</div>A mutable key whose <code>hashCode</code> changes after insertion is lost forever — you can\'t retrieve it. Keys should be immutable. <code>HashMap</code> allows one null key; <code>ConcurrentHashMap</code> and <code>TreeMap</code> do not.</div>'},
    {name:'Map variants & ConcurrentHashMap',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CHOOSE</span></div><pre>HashMap            O(1), no order, one null key, NOT thread-safe.\nLinkedHashMap      Insertion (or access) order. Great for LRU caches.\nTreeMap            Sorted by key, O(log n), NavigableMap methods.\nConcurrentHashMap  Thread-safe, lock-striping, no null keys/values.\n                   High concurrency without locking the whole map.\nHashtable          Legacy, fully synchronized, slow. Avoid.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang java">LRU CACHE in ~6 lines</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>class LRU&lt;K,V&gt; extends LinkedHashMap&lt;K,V&gt; {\n    private final int cap;\n    LRU(int cap){ super(16, 0.75f, true); this.cap = cap; } // accessOrder=true\n    @Override protected boolean removeEldestEntry(Map.Entry&lt;K,V&gt; e){\n        return size() &gt; cap;\n    }\n}</pre></div>'},
    {name:'Generics — Bounds, Wildcards, PECS',tag:'adv',body:'<div class="explanation">Generics give compile-time type safety and remove casts. The key advanced concept is variance via wildcards, summarized by <strong>PECS: Producer Extends, Consumer Super</strong>.</div><div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// &lt;? extends T&gt; = read-only producer (you GET T out)\ndouble sum(List&lt;? extends Number&gt; nums) {\n    double t = 0; for (Number n : nums) t += n.doubleValue(); return t;\n}                          // accepts List&lt;Integer&gt;, List&lt;Double&gt;\n\n// &lt;? super T&gt; = write consumer (you PUT T in)\nvoid addInts(List&lt;? super Integer&gt; dst) {\n    dst.add(1); dst.add(2);  // accepts List&lt;Integer&gt;, List&lt;Number&gt;, List&lt;Object&gt;\n}\n\n// bounded type parameter\n&lt;T extends Comparable&lt;T&gt;&gt; T max(List&lt;T&gt; list) {\n    return list.stream().max(Comparator.naturalOrder()).orElseThrow();\n}</pre></div><div class="gotcha"><div class="gotcha-label">Type erasure</div>Generics are a compile-time feature — at runtime <code>List&lt;String&gt;</code> and <code>List&lt;Integer&gt;</code> are both just <code>List</code>. Consequences: no <code>new T()</code>, no <code>T[]</code> creation, no <code>instanceof List&lt;String&gt;</code>, and overloads differing only by generic type won\'t compile.</div>'},
    {name:'Comparable vs Comparator & Sorting',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang java">JAVA</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>// Comparable: natural ordering, ONE per class (compareTo)\nrecord Person(String name, int age) implements Comparable&lt;Person&gt; {\n    public int compareTo(Person o) { return Integer.compare(age, o.age); }\n}\n\n// Comparator: external, many orderings, composable\nList&lt;Person&gt; people = new ArrayList&lt;&gt;(...);\npeople.sort(Comparator.comparing(Person::name)\n                      .thenComparing(Person::age).reversed());\n\npeople.sort(Comparator.comparingInt(Person::age)); // avoids boxing</pre></div><div class="gotcha"><div class="gotcha-label">Contract</div><code>compareTo</code>/<code>compare</code> must be consistent with <code>equals</code> and return a value whose sign is transitive — returning <code>a - b</code> for ints can overflow; use <code>Integer.compare(a,b)</code>.</div>'}
  ]
});
