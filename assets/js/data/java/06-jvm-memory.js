window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'JVM & Memory',
  number:'TAB 6',
  title:'JVM, Memory & Performance',
  color:'var(--accent2)',
  what:'What happens beneath your code: <em>JVM architecture, memory regions, garbage collection, class loading, JIT</em>, and how to diagnose <code>OutOfMemoryError</code>, leaks, and GC pauses. Strong signal of seniority.',
  topics:[
    {name:'JVM Architecture & Memory Regions',tag:'inter',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">RUNTIME DATA AREAS</span></div><pre>Heap        Shared. All objects + arrays. GC-managed.\n            Young gen (Eden + 2 Survivor) | Old gen (tenured)\n\nStack       Per-thread. Frames: local vars, partial results, operand stack.\n            StackOverflowError when too deep (recursion).\n\nMetaspace   Class metadata (replaced PermGen in Java 8). Native memory.\n\nPC Register Per-thread. Address of current instruction.\n\nNative Stack For native (JNI) method calls.\n\nFlow: javac -&gt; .class bytecode -&gt; ClassLoader -&gt; verify -&gt;\n      interpreter runs, JIT compiles hot paths to native code.</pre></div><div class="gotcha"><div class="gotcha-label">Stack vs Heap</div>Primitives &amp; object <em>references</em> live on the stack (per method frame); the actual objects live on the heap. Stack memory is reclaimed when the method returns; heap memory is reclaimed by GC.</div>'},
    {name:'Garbage Collection',tag:'inter',body:'<div class="explanation">GC reclaims unreachable objects. Based on the <strong>weak generational hypothesis</strong>: most objects die young. Minor GC cleans the young gen (fast); major/full GC touches the old gen (slower, can pause).</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">COLLECTORS</span></div><pre>Serial      Single-threaded. Tiny heaps / single core.\nParallel    Throughput-focused, multi-threaded. (was default pre-9)\nG1 GC       Default (Java 9+). Region-based, predictable pause targets.\nZGC / Shenandoah  Ultra-low pause (&lt;1-10ms), huge heaps. Concurrent.\n\nReachability: GC roots = active threads, static fields, JNI refs, stack vars.\nAn object is collectable when no chain of references reaches it from a root.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>You don\'t free memory in Java, but you can leak it logically — by holding references you no longer need (static collections, unremoved listeners, ThreadLocals in pools, unbounded caches). GC can\'t collect what\'s still reachable.</div>'},
    {name:'Diagnosing OOM & Memory Leaks',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">DIAGNOSTICS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># auto heap dump on OOM\n-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/heap.hprof\n\njps                       # list JVM processes\njmap -histo:live &lt;pid&gt;     # live object histogram (what\'s filling heap)\njmap -dump:live,format=b,file=heap.hprof &lt;pid&gt;\njstack &lt;pid&gt;               # thread dump (deadlocks, hot threads)\njstat -gcutil &lt;pid&gt; 1000   # GC stats every 1s\n\n# Analyze heap.hprof in Eclipse MAT / VisualVM -&gt; find dominator tree,\n# leak suspects, retained size.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">COMMON ERRORS</span></div><pre>OutOfMemoryError: Java heap space    too many live objects / leak / heap too small\nOutOfMemoryError: Metaspace          classloader leak (redeploys, dynamic proxies)\nOutOfMemoryError: GC overhead limit  98% time in GC, &lt;2% reclaimed\nStackOverflowError                   infinite/too-deep recursion</pre></div>'},
    {name:'Class Loading & JIT',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CLASS LOADERS (delegation model)</span></div><pre>Bootstrap  -&gt;  Platform  -&gt;  Application(System)  -&gt;  Custom\n\nDelegation: a loader asks its PARENT first; only loads itself if the\nparent can\'t. Prevents core classes being overridden.\n\nLifecycle: Load -&gt; Link (verify, prepare, resolve) -&gt; Initialize (static init).\n\nJIT: interpreter runs bytecode; HotSpot profiles &quot;hot&quot; methods and compiles\nthem to optimized native code (C1 client, C2 server). Optimizations:\ninlining, escape analysis (stack-allocate non-escaping objects),\nloop unrolling, dead-code elimination.</pre></div>'},
    {name:'Performance & JVM Tuning Flags',tag:'adv',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">COMMON FLAGS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>-Xms2g -Xmx2g            # initial = max heap (avoid resize pauses)\n-XX:+UseG1GC             # collector (default 9+)\n-XX:MaxGCPauseMillis=200 # G1 pause target\n-XX:MaxMetaspaceSize=256m\n-XX:+UseStringDeduplication\n-XX:+ExitOnOutOfMemoryError   # fail fast in containers\n\n# Containers (Java 10+ is container-aware):\n-XX:MaxRAMPercentage=75.0    # heap as % of container memory limit</pre></div><div class="phrase"><div class="phrase-label">Methodology</div>Measure before tuning: profile (async-profiler, JFR), find the real bottleneck, change one variable, re-measure. Premature optimization without data is guessing. Set <code>-Xms == -Xmx</code> in servers to avoid heap-resize jitter.</div>'}
  ]
});
