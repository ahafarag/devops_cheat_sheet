window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Containers',
  number:'AREA 6',
  title:'Containers & Docker',
  color:'var(--accent6)',
  what:'Complete containerization coverage: Docker internals, images, networking, storage, Compose, registries (ECR, ACR, Docker Hub), on-premises vs cloud-hosted container platforms.',
  topics:[
    {name:'Docker Internals — How Containers Actually Work',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CONTAINER INTERNALS</span></div><pre>WHAT IS A CONTAINER:\n  = Process with isolated namespaces + resource limits + filesystem\n  NOT a VM — no hypervisor, shares host kernel\n\nNAMESPACES (isolation):\n  PID:      Container sees only its own processes (PID 1 = entrypoint)\n  Network:  Own network stack (interfaces, routing, ports)\n  Mount:    Own filesystem view (union mount)\n  UTS:      Own hostname\n  IPC:      Own shared memory / message queues\n  User:     Map container root to unprivileged host user\n\nCGROUPS (resource limits):\n  CPU:     --cpus 0.5 (half a core), --cpu-shares (relative weight)\n  Memory:  --memory 512m (hard limit, OOM killed if exceeded)\n  I/O:     --device-read-bps, --blkio-weight\n  PIDs:    --pids-limit (prevent fork bombs)\n\nUNION FILESYSTEM (overlay2):\n  Image = stack of read-only layers\n  Container = image layers + thin writable layer on top\n  Copy-on-write: file modified → copied to writable layer\n  Each Dockerfile instruction = new layer (cached by content hash)\n\nCONTAINER RUNTIME EVOLUTION:\n  Docker Engine → containerd → runc (OCI runtime)\n  Kubernetes dropped Docker shim in 1.24 → uses containerd directly\n  OCI Standard: image spec + runtime spec (portability)</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">Docker container vs VM?</div><div class="q-answer"><strong>VM</strong>: full OS + kernel, hypervisor, heavy (GBs), minutes to start, strong isolation. <strong>Container</strong>: shares host kernel, namespaces for isolation, lightweight (MBs), seconds to start, weaker isolation. Containers for microservices/stateless. VMs when you need kernel-level isolation or different OS.</div></div>'},
{name:'Dockerfile — Best Practices & Multi-Stage Builds',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">DOCKERFILE — WRITE FROM MEMORY</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># ── Multi-stage build ──\nFROM python:3.11-slim AS builder\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir --user -r requirements.txt\n\nFROM python:3.11-slim\nWORKDIR /app\nRUN useradd -r appuser\nCOPY --from=builder /root/.local /root/.local\nCOPY . .\nENV PATH=/root/.local/bin:$PATH\nEXPOSE 8000\nUSER appuser\nHEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD curl -f http://localhost:8000/health || exit 1\nCMD ["gunicorn", "app:app", "--bind", "0.0.0.0:8000"]</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">BEST PRACTICES</span></div><pre>1. Specific base tags     python:3.11-slim NOT python:latest\n2. Multi-stage builds     Build in fat image, copy to slim runtime\n3. .dockerignore          Exclude .git, __pycache__, node_modules\n4. One process per container\n5. Non-root user          RUN useradd -r appuser && USER appuser\n6. Layer ordering         COPY requirements BEFORE code (cache deps)\n7. --no-cache-dir         On pip (smaller image)\n8. Combine RUN commands   Fewer layers, smaller image\n9. HEALTHCHECK            For orchestrator integration\n10. Use exec form CMD     CMD ["app"] not CMD app (PID 1 signals)\n\nIMAGE SIZE REDUCTION:\n  - Multi-stage: 1.2GB → ~150MB typical\n  - Alpine base: ~5MB (but musl libc compatibility issues)\n  - Distroless: Google\'s minimal images (no shell, minimal attack surface)\n  - Dive tool: analyze layer sizes (dive myimage:tag)</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">How do you reduce Docker image size?</div><div class="q-answer">1. Multi-stage builds. 2. Slim/alpine/distroless base. 3. Combine RUN layers. 4. --no-cache-dir, npm ci --production. 5. .dockerignore. 6. Remove build tools in same layer. 7. Use \'dive\' to analyze layers.</div></div>'},
{name:'Docker CLI — Every Command They\'ll Ask',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">DOCKER CLI REFERENCE</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># ── Build & Run ──\ndocker build -t myapp:1.0 .\ndocker build -t myapp:1.0 --no-cache .\ndocker run -d --name web -p 8080:8000 \\\n  -e DB_HOST=postgres -v appdata:/app/data \\\n  --restart unless-stopped --memory 512m --cpus 0.5 myapp:1.0\n\n# ── Debug (MOST IMPORTANT) ──\ndocker logs web                    # stdout/stderr\ndocker logs web --tail 100 -f      # follow\ndocker exec -it web /bin/sh        # shell into running container\ndocker inspect web                 # full JSON (network, mounts, env)\ndocker stats                       # live CPU/memory per container\ndocker top web                     # processes inside container\ndocker diff web                    # filesystem changes vs image\n\n# ── Images ──\ndocker images                      docker image prune -a\ndocker history myapp:1.0           # layer sizes\ndocker save myapp:1.0 | gzip > myapp.tar.gz\ndocker load < myapp.tar.gz\n\n# ── Cleanup ──\ndocker system prune -a --volumes   # nuclear cleanup\ndocker container prune             docker volume prune</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">Container keeps crashing. Debug?</div><div class="q-answer">docker logs container → check app errors, OOM, missing env. docker inspect → State.ExitCode (137=OOM, 1=app error). docker stats → resource pressure. If exits too fast: docker run -it --entrypoint /bin/sh image to debug manually. Check mounted configs/secrets.</div></div>'},
{name:'Docker Networking & Storage',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">NETWORKING MODES</span></div><pre>bridge:   Default. Private IP, NAT to host. Containers on same bridge\n          talk by name (Docker DNS). Good for single-host.\nhost:     Share host network stack. No isolation, fast. Use for performance.\nnone:     No networking at all.\noverlay:  Multi-host networking (Swarm/K8s). VXLAN tunnels.\nmacvlan:  Container gets MAC address on physical network. Legacy apps.\n\n# Create custom network (best practice)\ndocker network create --driver bridge my-app-net\ndocker run --network my-app-net --name api myapp\ndocker run --network my-app-net --name db postgres\n# api can reach db by name: postgres://db:5432</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">STORAGE</span></div><pre>Volumes:      Docker-managed, persist beyond container lifecycle\n              docker volume create mydata\n              docker run -v mydata:/app/data\n              Best for: databases, persistent state\n\nBind mounts:  Map host path → container path\n              docker run -v /host/path:/container/path\n              Best for: dev (live code reload), configs\n\ntmpfs:        In-memory only, no persistence\n              docker run --tmpfs /tmp\n              Best for: secrets, temp data (fast, secure)\n\nRULE: Volumes for persistence, bind mounts for dev, tmpfs for secrets</pre></div>'},
{name:'Docker Compose — Multi-Container Apps',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">DOCKER COMPOSE — WRITE FROM MEMORY</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># docker-compose.yml\nversion: \'3.8\'\nservices:\n  api:\n    build: .\n    ports: ["8000:8000"]\n    environment:\n      DB_HOST: postgres\n      REDIS_URL: redis://cache:6379\n    depends_on:\n      postgres: { condition: service_healthy }\n      cache: { condition: service_started }\n    restart: unless-stopped\n    healthcheck:\n      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]\n      interval: 30s\n      timeout: 5s\n      retries: 3\n\n  postgres:\n    image: postgres:16-alpine\n    volumes: [pgdata:/var/lib/postgresql/data]\n    environment:\n      POSTGRES_DB: myapp\n      POSTGRES_PASSWORD_FILE: /run/secrets/db_pass\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U postgres"]\n      interval: 10s\n\n  cache:\n    image: redis:7-alpine\n    ports: ["6379:6379"]\n\nvolumes:\n  pgdata:</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang bash">COMPOSE CLI</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>docker compose up -d              # start all\ndocker compose down               # stop and remove\ndocker compose logs -f api        # follow one service\ndocker compose ps                 # status\ndocker compose exec api sh        # shell into service\ndocker compose build --no-cache   # rebuild\ndocker compose pull               # pull latest images</pre></div>'},
{name:'Container Registries — ECR, ACR, Docker Hub',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">REGISTRY OPERATIONS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># ── AWS ECR ──\naws ecr get-login-password --region eu-central-1 | \\\n  docker login --username AWS --password-stdin ACCOUNT.dkr.ecr.eu-central-1.amazonaws.com\ndocker tag myapp:1.0 ACCOUNT.dkr.ecr.eu-central-1.amazonaws.com/myapp:1.0\ndocker push ACCOUNT.dkr.ecr.eu-central-1.amazonaws.com/myapp:1.0\n\n# ECR lifecycle policy (auto-cleanup old images)\naws ecr put-lifecycle-policy --repository-name myapp --lifecycle-policy-text \'{\n  "rules": [{"rulePriority":1,"selection":{"tagStatus":"untagged","countType":"sinceImagePushed","countNumber":14,"countUnit":"days"},"action":{"type":"expire"}}]\n}\'\n\n# ECR vulnerability scanning\naws ecr describe-image-scan-findings --repository-name myapp --image-id imageTag=latest\n\n# ── Azure ACR ──\naz acr login --name myregistry\ndocker tag myapp:1.0 myregistry.azurecr.io/myapp:1.0\ndocker push myregistry.azurecr.io/myapp:1.0\n\n# ── Docker Hub ──\ndocker login\ndocker tag myapp:1.0 username/myapp:1.0\ndocker push username/myapp:1.0</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">REGISTRY COMPARISON</span></div><pre>ECR:        AWS native, IAM auth, built-in scanning, lifecycle policies\n            Cross-region replication. Tight ECS/EKS integration.\nACR:        Azure native, AAD auth, geo-replication, Tasks (build in cloud)\n            Tight AKS integration.\nDocker Hub: Public/private repos. Rate limits on free tier (100 pulls/6hr).\n            GitHub Actions integration.\nHarbor:     Self-hosted open source. Replication, scanning, RBAC.\n            Good for on-prem/air-gapped.\nGHCR:       GitHub Container Registry. Free for public repos.\n            Native GitHub Actions integration.</pre></div>'},
{name:'On-Prem vs Cloud Container Platforms',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">COMPARISON</span></div><pre>ON-PREMISES:\n  Docker CE/EE on bare metal or VMs\n  Docker Swarm (simpler) or K8s (complex but powerful)\n  Private registry: Harbor, Nexus, or DTR\n  You manage: hardware, networking, storage, upgrades, security patches\n  Pros: full control, data sovereignty, no cloud costs\n  Cons: operational burden, scaling is manual/slow\n\nAWS:\n  ECS Fargate:  Serverless containers (no EC2 to manage)\n  ECS on EC2:   You manage instances, more control\n  EKS:          Managed Kubernetes (see K8s tab)\n  App Runner:   Simplest — from source/image to URL\n  Pros: auto-scaling, managed infra, integrated monitoring\n\nAZURE:\n  ACI:          Serverless containers (like Fargate)\n  ACA:          Container Apps — serverless K8s abstractions\n  AKS:          Managed Kubernetes (see K8s tab)\n  Pros: tight AD integration, hybrid with Arc\n\nWHEN TO USE WHAT:\n  Simple stateless app → Fargate / ACI / App Runner\n  Microservices at scale → EKS / AKS\n  On-prem requirement → K8s + Harbor\n  Lift-and-shift → ECS on EC2</pre></div>'}
  ]
});
