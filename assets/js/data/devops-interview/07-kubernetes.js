window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Kubernetes',
  number:'AREA 7',
  title:'Kubernetes — Complete Guide',
  color:'var(--accent7)',
  what:'End-to-end Kubernetes: architecture, objects, networking, storage, RBAC, Helm, EKS (AWS), AKS (Azure), troubleshooting, and production best practices.',
  topics:[
    {name:'K8s Architecture — Control Plane & Nodes',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">KUBERNETES ARCHITECTURE</span></div><pre>CONTROL PLANE:\n  kube-apiserver:           ALL communication goes through here (REST API)\n  etcd:                     Key-value store — source of truth for cluster state\n  kube-scheduler:           Assigns pods to nodes (resources, affinity, taints)\n  kube-controller-manager:  Reconciliation loops:\n    - ReplicaSet controller: ensures N pods running\n    - Node controller: detects node failures\n    - Job controller: batch jobs\n    - Endpoint controller: populates Service endpoints\n  cloud-controller-manager: Cloud-specific logic (LBs, storage, nodes)\n\nNODE COMPONENTS:\n  kubelet:           Ensures containers run per PodSpec. Reports node status.\n  kube-proxy:        Network rules (iptables/IPVS) for Service routing.\n  Container runtime: containerd or CRI-O (Docker shim removed in 1.24)\n\nCOMMUNICATION FLOW:\n  User → kubectl → kube-apiserver → etcd (store)\n  kube-apiserver → kubelet (schedule pod)\n  kubelet → container runtime (start container)\n  kube-proxy → iptables (route traffic to pods)\n\nAPI GROUPS:\n  core/v1:    Pod, Service, ConfigMap, Secret, PV, PVC, Namespace\n  apps/v1:    Deployment, StatefulSet, DaemonSet, ReplicaSet\n  batch/v1:   Job, CronJob\n  networking: Ingress, NetworkPolicy\n  autoscaling: HPA</pre></div>'},
{name:'Core Objects — Pod, Deployment, Service, Ingress',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">K8S OBJECT HIERARCHY</span></div><pre>POD:\n  Smallest deployable unit. One or more containers sharing network/storage.\n  Ephemeral — never create pods directly, use Deployments.\n\nDEPLOYMENT → REPLICASET → POD:\n  Deployment manages desired state (replicas, image version)\n  ReplicaSet ensures N pods running (auto-created by Deployment)\n  Rolling update: maxSurge=1, maxUnavailable=0 → zero-downtime\n\nSERVICE (stable network endpoint for pods):\n  ClusterIP:     Internal only (default). DNS: service-name.namespace.svc.cluster.local\n  NodePort:      Expose on every node\'s IP:port (30000-32767)\n  LoadBalancer:  Cloud LB (ALB/NLB on AWS, Azure LB on AKS)\n  Headless:      clusterIP: None — direct pod DNS (for StatefulSets)\n\nINGRESS:\n  HTTP/HTTPS routing rules (path/host → service)\n  Requires Ingress Controller (nginx, ALB, Traefik)\n  TLS termination, virtual hosts, path-based routing\n\nCONFIGMAP & SECRET:\n  ConfigMap:   Non-sensitive config (env vars, config files)\n  Secret:      Sensitive data (base64 encoded, NOT encrypted by default!)\n  Mount as env vars or volume files\n\nSTATEFULSET:\n  For stateful apps (databases, Kafka, Redis)\n  Stable hostname: web-0, web-1, web-2 (not random like Deployments)\n  Ordered start/stop, persistent volume per pod\n\nDAEMONSET:\n  One pod per node (monitoring agents, log collectors, node-level services)\n  Fluentd, Datadog agent, kube-proxy itself\n\nJOB / CRONJOB:\n  Job: run-to-completion (batch processing, migrations)\n  CronJob: scheduled jobs (backups, cleanup)</pre></div>'},
{name:'K8s YAML Manifests — Write From Memory',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">DEPLOYMENT + SERVICE + INGRESS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web-api\nspec:\n  replicas: 3\n  selector:\n    matchLabels: { app: web-api }\n  strategy:\n    type: RollingUpdate\n    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }\n  template:\n    metadata:\n      labels: { app: web-api }\n    spec:\n      containers:\n      - name: api\n        image: myregistry/web-api:1.2.3\n        ports:\n        - containerPort: 8000\n        env:\n        - name: DB_HOST\n          valueFrom:\n            configMapKeyRef: { name: app-config, key: db_host }\n        - name: DB_PASSWORD\n          valueFrom:\n            secretKeyRef: { name: app-secrets, key: db_password }\n        resources:\n          requests: { cpu: 100m, memory: 128Mi }\n          limits:   { cpu: 500m, memory: 512Mi }\n        livenessProbe:\n          httpGet: { path: /health, port: 8000 }\n          initialDelaySeconds: 10\n          periodSeconds: 15\n        readinessProbe:\n          httpGet: { path: /ready, port: 8000 }\n          initialDelaySeconds: 5\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: web-api\nspec:\n  selector: { app: web-api }\n  ports:\n  - port: 80\n    targetPort: 8000\n  type: ClusterIP\n---\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: web-api\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: web-api\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target: { type: Utilization, averageUtilization: 70 }</pre></div>'},
{name:'kubectl — Every Command You Need',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">KUBECTL REFERENCE</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># ── Get info ──\nkubectl get pods -o wide               # show node, IP\nkubectl get pods -A                     # all namespaces\nkubectl get deploy,svc,ing              # multiple types\nkubectl get pods -l app=web-api         # filter by label\nkubectl describe pod web-api-abc123     # events + details\nkubectl get events --sort-by=.metadata.creationTimestamp\n\n# ── Debugging ──\nkubectl logs web-api-abc123             # container logs\nkubectl logs web-api-abc123 --previous  # crashed container logs\nkubectl logs -f -l app=web-api --all-containers\nkubectl exec -it web-api-abc123 -- /bin/sh\nkubectl port-forward svc/web-api 8080:80\nkubectl top pods                        # CPU/memory\nkubectl top nodes\nkubectl run debug --image=busybox -it --rm -- /bin/sh  # temp debug pod\n\n# ── Apply & Manage ──\nkubectl apply -f deployment.yaml\nkubectl apply -f ./manifests/           # whole directory\nkubectl delete -f deployment.yaml\nkubectl scale deploy web-api --replicas=5\nkubectl rollout status deploy web-api\nkubectl rollout undo deploy web-api\nkubectl rollout undo deploy web-api --to-revision=3\n\n# ── Context ──\nkubectl config get-contexts\nkubectl config use-context prod-cluster\nkubectl config set-context --current --namespace=myapp</pre></div>'},
{name:'K8s Networking — Services, DNS, Network Policies',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">KUBERNETES NETWORKING MODEL</span></div><pre>RULES:\n  1. Every pod gets its own IP address\n  2. All pods can communicate with all other pods without NAT\n  3. Agents on a node can communicate with all pods on that node\n\nDNS:\n  service-name.namespace.svc.cluster.local\n  pod-ip-dashed.namespace.pod.cluster.local\n  CoreDNS resolves service names → ClusterIP\n\nCNI PLUGINS (Container Network Interface):\n  AWS VPC CNI:  Pods get real VPC IPs (native AWS networking)\n  Calico:       Network policies + BGP routing (on-prem + cloud)\n  Cilium:       eBPF-based, fast, advanced policies + observability\n  Flannel:      Simple overlay (good for learning, not production)\n  Weave:        Mesh network, easy setup\n\nNETWORK POLICIES:\n  Firewall rules for pods. Default: all traffic allowed.\n  Ingress rules: who can talk TO this pod\n  Egress rules: who can this pod talk TO</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang yaml">NETWORK POLICY EXAMPLE</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>apiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: api-policy\nspec:\n  podSelector:\n    matchLabels: { app: web-api }\n  policyTypes: [Ingress, Egress]\n  ingress:\n  - from:\n    - podSelector:\n        matchLabels: { app: frontend }\n    ports:\n    - port: 8000\n  egress:\n  - to:\n    - podSelector:\n        matchLabels: { app: database }\n    ports:\n    - port: 5432</pre></div>'},
{name:'K8s Storage — PV, PVC, StorageClass',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">STORAGE MODEL</span></div><pre>PersistentVolume (PV):     Actual storage resource (EBS, Azure Disk, NFS)\nPersistentVolumeClaim (PVC): Request for storage by a pod\nStorageClass:              Template for dynamic provisioning\n\nSTATIC:  Admin creates PV → user creates PVC → bound\nDYNAMIC: User creates PVC with StorageClass → PV auto-created\n\nACCESS MODES:\n  ReadWriteOnce (RWO): single node read-write (most common, EBS)\n  ReadOnlyMany (ROX):  many nodes read-only\n  ReadWriteMany (RWX): many nodes read-write (EFS, NFS, Azure Files)\n\nRECLAIM POLICIES:\n  Retain:  Keep PV after PVC deleted (manual cleanup)\n  Delete:  Delete PV and backing storage (default for dynamic)</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang yaml">PVC + STORAGECLASS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>apiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: postgres-data\nspec:\n  accessModes: [ReadWriteOnce]\n  storageClassName: gp3\n  resources:\n    requests:\n      storage: 50Gi\n---\n# AWS EBS StorageClass\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: gp3\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n  fsType: ext4\nreclaimPolicy: Delete\nvolumeBindingMode: WaitForFirstConsumer</pre></div>'},
{name:'K8s RBAC — Authentication & Authorization',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">RBAC MODEL</span></div><pre>WHO:   ServiceAccount, User, Group\nWHAT:  Role (namespaced) or ClusterRole (cluster-wide)\nBIND:  RoleBinding or ClusterRoleBinding\n\nServiceAccount: identity for pods (each namespace has \'default\')\nRole:           list of permissions (verbs + resources)\nRoleBinding:    grants Role to a subject in a namespace\nClusterRole:    cluster-wide permissions\nClusterRoleBinding: grants ClusterRole cluster-wide</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang yaml">RBAC EXAMPLE</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># Role: read pods in \'production\' namespace\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  namespace: production\n  name: pod-reader\nrules:\n- apiGroups: [""]\n  resources: ["pods", "pods/log"]\n  verbs: ["get", "list", "watch"]\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  namespace: production\n  name: read-pods\nsubjects:\n- kind: ServiceAccount\n  name: monitoring-agent\n  namespace: production\nroleRef:\n  kind: Role\n  name: pod-reader\n  apiGroup: rbac.authorization.k8s.io</pre></div>'},
{name:'Helm — Kubernetes Package Manager',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">HELM</span></div><pre>Chart = package of K8s manifests + Go templates + values\n\nmychart/\n  Chart.yaml       metadata\n  values.yaml      defaults\n  templates/       K8s manifest templates\n    deployment.yaml\n    service.yaml\n    _helpers.tpl   reusable template functions\n  charts/          sub-chart dependencies\n\nCOMMANDS:\n  helm install my-app ./mychart -f prod-values.yaml\n  helm upgrade my-app ./mychart -f prod-values.yaml\n  helm rollback my-app 2\n  helm list / helm history my-app\n  helm template my-app ./mychart     # render locally (debug)\n  helm lint ./mychart                # validate\n\nGO TEMPLATES:\n  {{ .Values.image.repository }}\n  {{ .Release.Name }}\n  {{- if .Values.ingress.enabled }}\n  {{- range .Values.hosts }}</pre></div>'},
{name:'AWS EKS — Managed Kubernetes on AWS',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">EKS ARCHITECTURE</span></div><pre>EKS = AWS manages the control plane (apiserver, etcd, scheduler)\nYou manage: worker nodes (or use Fargate for serverless)\n\nNODE OPTIONS:\n  Managed Node Groups:  EC2 instances managed by AWS (auto-updates, draining)\n  Self-managed:         You manage EC2 instances + AMI + scaling\n  Fargate:              Serverless — no nodes to manage, per-pod billing\n\nNETWORKING:\n  VPC CNI:              Pods get real VPC IPs (not overlay)\n                        Pod can talk to RDS, ElastiCache natively\n  Security Groups:      Can be applied per-pod (SG for Pods)\n  ALB Ingress:          AWS Load Balancer Controller → creates ALB from Ingress\n\nSTORAGE:\n  EBS CSI driver:       gp3/io2 volumes (ReadWriteOnce)\n  EFS CSI driver:       Shared filesystem (ReadWriteMany)\n\nAUTH:\n  aws-auth ConfigMap:   Maps IAM roles/users → K8s RBAC\n  IRSA:                 IAM Roles for Service Accounts (pod-level IAM)\n                        Pod assumes IAM role without node-level permissions\n\nMONITORING:\n  CloudWatch Container Insights\n  Prometheus + Grafana (via Helm charts)\n  AWS X-Ray for distributed tracing</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang bash">EKS CLI</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># Create cluster\neksctl create cluster --name prod --region eu-central-1 \\\n  --nodegroup-name workers --node-type t3.large --nodes 3 \\\n  --managed --with-oidc\n\n# Update kubeconfig\naws eks update-kubeconfig --name prod --region eu-central-1\n\n# Install ALB controller\nhelm install aws-load-balancer-controller \\\n  eks/aws-load-balancer-controller -n kube-system \\\n  --set clusterName=prod\n\n# IRSA: create IAM role for a service account\neksctl create iamserviceaccount --name s3-reader \\\n  --namespace default --cluster prod \\\n  --attach-policy-arn arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">How do pods access AWS services securely in EKS?</div><div class="q-answer"><strong>IRSA (IAM Roles for Service Accounts)</strong>: Create IAM role with policy → associate with K8s ServiceAccount via OIDC → pods using that SA automatically get temporary AWS credentials via STS. No static keys, no node-level permissions. Principle of least privilege per-pod.</div></div>'},
{name:'Azure AKS — Managed Kubernetes on Azure',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">AKS ARCHITECTURE</span></div><pre>AKS = Azure manages control plane (free — you pay only for nodes)\n\nNODE POOLS:\n  System pool:     Core components (CoreDNS, metrics-server). Always on.\n  User pool:       Your workloads. Can auto-scale (cluster autoscaler).\n  Virtual nodes:   Serverless via Azure Container Instances (like Fargate)\n  Spot instances:  Cheap, interruptible (batch workloads)\n\nNETWORKING:\n  kubenet:         Basic. Pods get IPs from separate CIDR, NAT to VNet.\n  Azure CNI:       Pods get VNet IPs directly (like AWS VPC CNI).\n  Azure CNI Overlay: VNet integration without IP exhaustion.\n\nSTORAGE:\n  Azure Disk:      Block storage (ReadWriteOnce) — managed disks\n  Azure Files:     SMB/NFS share (ReadWriteMany) — shared access\n  Azure Blob:      Object storage via CSI driver\n\nAUTH & IDENTITY:\n  Azure AD integration:   AAD users/groups → K8s RBAC\n  Workload Identity:      Azure equivalent of IRSA\n                          Pod → Federated credential → Managed Identity\n  Managed Identity:       Cluster-level identity for Azure resources\n\nKEY DIFFERENCES FROM EKS:\n  Free control plane (EKS charges ~$0.10/hr)\n  Tighter AD/Entra integration\n  Azure Policy for K8s (OPA Gatekeeper built-in)\n  Start/Stop cluster feature (dev cost savings)</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang bash">AKS CLI</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># Create cluster\naz aks create -g myResourceGroup -n myAKS \\\n  --node-count 3 --node-vm-size Standard_D4s_v5 \\\n  --enable-managed-identity --network-plugin azure \\\n  --generate-ssh-keys\n\n# Get credentials\naz aks get-credentials -g myResourceGroup -n myAKS\n\n# Scale node pool\naz aks nodepool scale -g myResourceGroup --cluster-name myAKS \\\n  -n nodepool1 --node-count 5\n\n# Enable monitoring\naz aks enable-addons -g myResourceGroup -n myAKS -a monitoring</pre></div>'},
{name:'K8s Troubleshooting — Common Issues',tag:'must',body:'<div class="q-block"><div class="q-label">CrashLoopBackOff</div><div class="q-text">Pod keeps restarting</div><div class="q-answer">kubectl describe pod → Events. kubectl logs --previous → last crash output. Check: OOMKilled (increase memory limits), missing ConfigMap/Secret, wrong image tag, failed liveness probe, app startup error. Debug: kubectl run debug --image=same-image -it -- /bin/sh</div></div><div class="q-block"><div class="q-label">ImagePullBackOff</div><div class="q-text">Can\'t pull container image</div><div class="q-answer">Check: image name/tag typo, registry auth (imagePullSecrets), ECR login expired, private registry not accessible from cluster, network policy blocking egress to registry.</div></div><div class="q-block"><div class="q-label">Pending Pod</div><div class="q-text">Pod stuck in Pending</div><div class="q-answer">kubectl describe pod → check Events. Common: insufficient CPU/memory (scale up nodes or reduce requests), no matching node (affinity/taint), PVC can\'t bind (no matching PV or StorageClass).</div></div><div class="q-block"><div class="q-label">Service Not Reachable</div><div class="q-text">Can\'t access service from outside</div><div class="q-answer">kubectl get svc → check type and external IP. kubectl get endpoints → are there pod IPs? (empty = selector doesn\'t match pod labels). Check Ingress controller running, annotations correct, TLS cert valid. Test: kubectl port-forward svc/name 8080:80 → works? Then it\'s LB/Ingress issue.</div></div>'}
  ]
});
