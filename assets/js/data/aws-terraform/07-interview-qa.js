window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Interview Q&A',
  number: 'TAB 7',
  title: 'AWS & Terraform Interview Q&A',
  color: 'var(--accent7)',
  what: 'High-frequency AWS and Terraform interview questions grouped by area, with model answers and a couple of scenario walkthroughs. Cover the question first, then check your answer.',
  topics:[
    {name:'AWS Core Q&A',tag:'must',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Region vs Availability Zone — and how do you design for failure?</div><div class="q-answer">A Region is an isolated geography; an AZ is one+ data centers within it with independent power/network. Design for AZ failure by deploying across &ge;2 AZs (multi-AZ). Only go multi-Region when DR requirements (RTO/RPO) justify the added cost and complexity.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Explain IAM policy evaluation.</div><div class="q-answer">Default deny → evaluate all applicable policies → an explicit <strong>deny always wins</strong> → otherwise an explicit allow grants access only if it also passes SCPs and permission boundaries → no allow means implicit deny.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">IAM user vs role — which and why?</div><div class="q-answer">Prefer roles: they issue temporary STS credentials with no long-lived keys to leak. EC2 uses an instance profile, Lambda an execution role, EKS pods IRSA, and cross-account access uses assumable roles. Reserve users for break-glass or legacy, ideally replaced by SSO/Identity Center.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Security Group vs NACL?</div><div class="q-answer">SG = stateful, instance/ENI level, allow-only, all rules evaluated — your primary control. NACL = stateless, subnet level, allow+deny, ordered first-match — a coarse guardrail. Stateless means NACLs need explicit return-traffic rules (ephemeral ports).</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you secure an S3 bucket?</div><div class="q-answer">Keep Block Public Access on, serve via CloudFront with Origin Access Control, enforce SSE-KMS encryption and TLS via bucket policy, scope IAM least-privilege, enable versioning + access logging, and use VPC gateway endpoints to keep traffic private.</div></div>'},
    {name:'AWS Architecture Q&A',tag:'likely',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Multi-AZ vs read replicas in RDS?</div><div class="q-answer">Multi-AZ = high availability: a synchronous standby in another AZ with automatic failover; the standby isn\'t readable. Read replicas = read scaling: asynchronous, readable copies you can promote manually. Different problems — availability vs throughput.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">When Lambda vs ECS/Fargate vs EC2?</div><div class="q-answer">Lambda for event-driven, spiky, short (&lt;15 min) work with zero ops. ECS/Fargate for containerized services without managing nodes. EKS when you need Kubernetes/portability. EC2 for full control, special hardware, or steady high load where always-on compute is cheaper.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">RDS vs DynamoDB?</div><div class="q-answer">RDS for relational data, joins, transactions, ad-hoc queries, known schema. DynamoDB for internet-scale, predictable key-based access with single-digit-ms latency and serverless ops — but you must design tables around access patterns; it\'s weak at joins/ad-hoc queries.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you reduce NAT Gateway and data-transfer cost?</div><div class="q-answer">Add VPC gateway endpoints for S3/DynamoDB (free, keeps traffic off NAT), use interface endpoints/PrivateLink for other services, keep chatty workloads in the same AZ to avoid cross-AZ charges, and cache with CloudFront.</div></div>'},
    {name:'Terraform Q&A',tag:'must',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What is Terraform state and why does it matter?</div><div class="q-answer">State maps your config to real resource IDs and caches attributes so <code>plan</code> can diff desired vs actual. It can hold secrets (treat as sensitive), must live in a remote backend for teams, and must never be hand-edited — use state commands and <code>import</code>.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you manage state for a team safely?</div><div class="q-answer">Remote backend (e.g. S3) with encryption + versioning, state locking (DynamoDB table or native S3 lockfile in 1.10+) to prevent concurrent-apply corruption, and split state per environment/component to limit blast radius.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">count vs for_each?</div><div class="q-answer">Both create multiple instances. <code>count</code> is index-based, so removing a middle element re-indexes and recreates resources. <code>for_each</code> keys by a stable map/set value, so add/remove touches only that key. Use for_each for distinct named resources; count for N-identical or conditional creation.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">terraform plan vs apply — and how do you do safe applies?</div><div class="q-answer"><code>plan</code> previews the diff without changing anything; <code>apply</code> executes it. For safety: run plan in CI on PRs, save the plan file and apply exactly that artifact, require approval before prod apply, and use locking so applies serialize.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you bring existing infra under Terraform?</div><div class="q-answer"><code>terraform import</code> (or <code>import</code> blocks in 1.5+) maps a real resource into state, then you write matching config and run <code>plan</code> until there\'s no diff. For drift, reconcile through code rather than editing state.</div></div>'},
    {name:'Scenarios & Best Practices Q&A',tag:'likely',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you structure a Terraform repo for multiple environments?</div><div class="q-answer">Reusable <code>modules/</code> plus per-environment roots (<code>envs/dev</code>, <code>envs/prod</code>) each with its own backend/state and tfvars. This isolates blast radius and variables. Workspaces are a lighter alternative but riskier for prod since they share one config.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">A teammate changed a resource in the AWS console. What happens on next plan?</div><div class="q-answer">Terraform detects drift and shows a diff to revert it to the configured state. Fix by codifying the change, or use <code>import</code>/<code>ignore_changes</code> if it\'s intentionally managed elsewhere. Don\'t hand-edit state.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you keep secrets out of Terraform?</div><div class="q-answer">Don\'t hard-code them. Source from AWS Secrets Manager / SSM Parameter Store via data sources, pass via <code>TF_VAR_*</code> in CI, mark variables/outputs <code>sensitive</code>, and remember state itself can contain secrets — encrypt the backend and restrict access.</div></div><div class="q-block"><div class="q-label">Scenario</div><div class="q-text">Design a highly available, IaC-managed web app on AWS.</div><div class="q-answer">VPC with public + private subnets across &ge;2 AZs; ALB in public subnets; app on ECS Fargate (or ASG) in private subnets across AZs; RDS Multi-AZ (or Aurora) in private subnets; S3 + CloudFront for static assets; Route53 with health checks; secrets in Secrets Manager; least-privilege IAM roles. Everything in Terraform modules with remote state + locking, planned in CI and applied with approval per environment.</div></div><div class="q-block"><div class="q-label">Scenario</div><div class="q-text">terraform apply failed halfway. What now?</div><div class="q-answer">State records what actually succeeded, so it\'s partially applied — re-run <code>plan</code> to see remaining work and <code>apply</code> again (Terraform is declarative/convergent). Investigate the root cause (quota, dependency, permissions). If a resource is wedged, <code>taint</code>/replace it; if state and reality diverged, reconcile with <code>state</code> commands or <code>import</code>.</div></div>'}
  ]
});
