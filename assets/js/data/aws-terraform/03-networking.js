window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Networking',
  number: 'TAB 3',
  title: 'VPC Networking',
  color: 'var(--accent6)',
  what: 'The network plumbing interviewers love to probe: <em>VPC, subnets, the Security-Group-vs-NACL distinction, NAT/IGW/endpoints, and load balancing + DNS</em>.',
  topics:[
    {name:'VPC, Subnets & Routing',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">VPC ANATOMY</span></div><pre>VPC            Isolated virtual network, a CIDR block (e.g. 10.0.0.0/16).\nSubnet         CIDR slice bound to ONE AZ. Public (route to IGW) or\n               Private (no direct internet route).\nRoute table    Per-subnet routing. 0.0.0.0/0 → IGW (public) or NAT (private).\nInternet GW    Horizontally-scaled gateway giving public subnets internet.\nNAT Gateway    Lets PRIVATE subnets reach OUT to internet (no inbound).\n\nPattern: public subnets = ALB / NAT; private subnets = app + DB tiers.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>A subnet is "public" only because its route table sends 0.0.0.0/0 to an Internet Gateway and instances have public IPs. App and DB tiers live in private subnets and egress via a NAT Gateway.</div>'},
    {name:'Security Groups vs NACLs',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">THE CLASSIC COMPARISON</span></div><pre>                 Security Group            Network ACL\nLevel            ENI / instance            Subnet\nState            Stateful (return traffic   Stateless (must allow both\n                 auto-allowed)              inbound &amp; outbound)\nRules            Allow only                 Allow AND Deny\nEvaluation       All rules (any match)      Numbered, in order, first match\nDefault          Deny inbound, allow out    Default ACL allows all\nUse              Primary control            Coarse subnet guardrail / block IPs</pre></div><div class="gotcha"><div class="gotcha-label">Most-asked gotcha</div>SGs are <strong>stateful</strong> — if you allow inbound 443, the response is automatically allowed out. NACLs are <strong>stateless</strong> — you must open the ephemeral return port range (1024-65535) explicitly. Forgetting that breaks connectivity.</div>'},
    {name:'NAT, Endpoints & Peering',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CONNECTIVITY</span></div><pre>VPC Endpoints   Reach AWS services WITHOUT internet/NAT:\n  Gateway       S3 &amp; DynamoDB (route-table entry, free).\n  Interface     Most services (PrivateLink ENI, hourly + data cost).\nVPC Peering     1:1 private connectivity between VPCs (no transitive).\nTransit Gateway Hub-and-spoke connecting many VPCs / on-prem at scale.\nSite-to-Site VPN / Direct Connect   On-prem connectivity.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>I add Gateway endpoints for S3/DynamoDB to keep traffic off the NAT Gateway — it cuts NAT data-processing cost and keeps data on the AWS backbone. For many VPCs I use Transit Gateway instead of a mesh of peerings (peering isn\'t transitive).</div>'},
    {name:'Load Balancing & DNS',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">ELB &amp; ROUTE53</span></div><pre>ALB (L7)   HTTP/HTTPS, path/host routing, WAF, target groups, TLS term.\nNLB (L4)   TCP/UDP, ultra-low latency, static IP, millions of conns.\nGWLB       Inserts virtual appliances (firewalls) in the path.\n\nRoute53    DNS + health checks + routing policies:\n           simple | weighted | latency | failover | geolocation |\n           multivalue. Alias records map to AWS resources (free, no TTL pain).</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">Trace a request from a user to your app.</div><div class="q-answer">User → Route53 DNS resolves to → CloudFront (edge cache + WAF) → ALB in public subnets (TLS termination, listener rules) → target group → app on EC2/ECS in private subnets → if needed, RDS in private subnets reached via security-group rules. VPC Flow Logs + CloudWatch capture the path.</div></div>'}
  ]
});
