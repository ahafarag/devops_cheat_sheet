window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Core & IAM',
  number: 'TAB 1',
  title: 'AWS Global Infrastructure & IAM',
  color: 'var(--accent1)',
  what: 'The foundation: <em>how AWS is laid out (Regions, AZs, edge), and how IAM actually evaluates a request</em>. IAM is the single most-asked AWS interview topic — know the evaluation order cold.',
  topics:[
    {name:'Global Infrastructure — Regions, AZs, Edge',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">LAYOUT</span></div><pre>Region            Isolated geographic area (e.g. eu-west-1). Pick for\n                  latency, data residency, cost, service availability.\nAvailability Zone One+ discrete data centers, isolated power/network.\n                  Deploy across &gt;= 2 AZs for high availability.\nEdge Locations    CloudFront CDN + Route53 + global accelerators.\nLocal Zones /     Latency-sensitive workloads close to metros / on-prem.\nOutposts\n\nRule: design for AZ failure (multi-AZ), and for Region failure only\nwhen the business requires it (multi-Region = more cost/complexity).</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Most services are Regional; a few are Global (IAM, Route53, CloudFront, WAF). High availability = spread across AZs; disaster recovery across Regions. I default to multi-AZ and only go multi-Region when RTO/RPO demands it.</div>'},
    {name:'IAM — Users, Roles, Policies',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">IDENTITIES &amp; POLICIES</span></div><pre>User      Long-lived identity (humans/legacy). Avoid access keys; prefer SSO.\nGroup     Collection of users; attach policies once.\nRole      Assumable identity with NO long-term creds — temporary tokens via STS.\n          Used by EC2/Lambda/EKS (instance profile / IRSA) and cross-account.\nPolicy    JSON: Effect, Action, Resource, Condition.\n          Identity-based | Resource-based (S3 bucket policy) |\n          Permission boundary | SCP (org guardrail) | Session policy.</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">POLICY DOCUMENT</span></div><pre>{ "Effect": "Allow",\n  "Action": ["s3:GetObject"],\n  "Resource": "arn:aws:s3:::my-bucket/*",\n  "Condition": { "IpAddress": { "aws:SourceIp": "10.0.0.0/8" } } }</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Prefer roles over users — EC2 gets an instance profile, Lambda an execution role, EKS pods use IRSA / Pod Identity. No static keys to leak, and STS hands out short-lived credentials.</div>'},
    {name:'IAM Policy Evaluation Logic',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">EVALUATION ORDER</span></div><pre>1. Default DENY (implicit).\n2. Evaluate ALL applicable policies (identity, resource, SCP,\n   permission boundary, session).\n3. An explicit DENY anywhere ALWAYS wins.\n4. Otherwise, an explicit ALLOW grants access\n   (must also pass SCP + permission boundary).\n5. No explicit allow → implicit deny.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">A user has an Allow policy but still gets AccessDenied. Why?</div><div class="q-answer">Something explicitly denies it or fails to allow it in another layer: an SCP at the org level, a permission boundary capping the identity, a resource policy (e.g. S3 bucket) not allowing them, or a Condition (IP/MFA) not met. Explicit deny and missing SCP/boundary allow both block.</div></div>'},
    {name:'Accounts, Organizations & Well-Architected',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">GOVERNANCE</span></div><pre>AWS Organizations   Multi-account: separate prod/dev/security/logging.\n                    Consolidated billing + SCPs as guardrails.\nControl Tower       Opinionated landing zone (account factory, baselines).\nShared Responsibility  AWS secures OF the cloud; you secure IN the cloud\n                    (IAM, patching, data, config).\nWell-Architected    6 pillars: Operational Excellence, Security,\n                    Reliability, Performance, Cost, Sustainability.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Multi-account is the AWS-recommended blast-radius boundary — I isolate environments into separate accounts, enforce guardrails with SCPs, and centralize logging/security tooling. It beats one giant account with IAM-only separation.</div>'}
  ]
});
