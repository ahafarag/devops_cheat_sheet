window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Terraform Adv',
  number: 'TAB 6',
  title: 'Terraform — Modules, Remote State & Scale',
  color: 'var(--accent4)',
  what: 'Production Terraform: <em>modules for reuse, remote state with locking, count vs for_each, workspaces, and lifecycle controls</em> — the patterns that keep a real codebase maintainable across environments and teams.',
  topics:[
    {name:'Modules — Reuse & Composition',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang hcl">calling a module</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>module "network" {\n  source  = "./modules/vpc"        # local | registry | git ref\n  # source = "terraform-aws-modules/vpc/aws"  version = "~&gt; 5.0"\n\n  cidr_block  = "10.0.0.0/16"\n  environment = var.environment\n}\n\n# consume module outputs\nresource "aws_instance" "app" {\n  subnet_id = module.network.private_subnet_ids[0]\n}</pre></div><div class="explanation">A module is a reusable folder of <code>.tf</code> files with <strong>inputs</strong> (variables) and <strong>outputs</strong>. The root module calls child modules. Pin module versions, keep them small and focused, and avoid leaking provider config into reusable modules.</div>'},
    {name:'Remote State & Locking',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang hcl">S3 backend</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>terraform {\n  backend "s3" {\n    bucket         = "my-tfstate"\n    key            = "prod/network/terraform.tfstate"\n    region         = "eu-west-1"\n    encrypt        = true\n    dynamodb_table = "tf-locks"     # state locking (classic)\n    # use_lockfile = true           # native S3 lock (Terraform 1.10+)\n  }\n}</pre></div><div class="explanation">A <strong>remote backend</strong> stores state centrally (encrypted) so a team shares one source of truth. <strong>State locking</strong> prevents two concurrent <code>apply</code>s from corrupting state — historically via a DynamoDB lock table; Terraform 1.10+ also supports native S3 lockfiles.</div><div class="phrase"><div class="phrase-label">Say this</div>I split state per environment/component (separate keys/backends) to limit blast radius, enable encryption + versioning on the state bucket, and require locking so concurrent applies can\'t race. Other stacks read values via <code>terraform_remote_state</code> or data sources.</div>'},
    {name:'count vs for_each',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang hcl">count vs for_each</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># count — index-addressed (aws_instance.web[0], [1] ...)\nresource "aws_instance" "web" {\n  count         = 3\n  instance_type = "t3.micro"\n}\n\n# for_each — key-addressed (stable across changes)\nresource "aws_s3_bucket" "b" {\n  for_each = toset(["logs", "assets", "backups"])\n  bucket   = "myapp-${each.key}"\n}</pre></div><div class="gotcha"><div class="gotcha-label">Why for_each is usually better</div>With <code>count</code>, removing a middle item re-indexes everything after it, so Terraform destroys/recreates resources that just shifted position. <code>for_each</code> keys resources by a stable string, so adding/removing one item only touches that one. Use <code>count</code> for "N identical" or conditional (<code>count = var.enabled ? 1 : 0</code>).</div>'},
    {name:'Workspaces & Environment Layout',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">workspaces</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>terraform workspace new staging\nterraform workspace select staging\n# terraform.workspace → "staging" (use in names/conditionals)</pre></div><div class="explanation">Workspaces give multiple state instances from one config (default/staging/prod). They\'re handy for lightweight parallel copies, but many teams instead use <strong>separate directories + backends per environment</strong> for stronger isolation and per-env variables.</div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">Workspaces vs separate state files per environment?</div><div class="q-answer">Workspaces share one backend/config and differ only by state — easy but risky for prod (one bad config change can hit all). Separate dirs/backends per env give clear isolation, distinct variables, and independent blast radius — preferred for prod-grade setups.</div></div>'},
    {name:'Lifecycle, Dynamic Blocks & Provisioners',tag:'possible',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang hcl">lifecycle + dynamic</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>resource "aws_instance" "app" {\n  # ...\n  lifecycle {\n    create_before_destroy = true   # avoid downtime on replace\n    prevent_destroy       = true   # guard critical resources\n    ignore_changes        = [tags["LastModified"]]\n  }\n}\n\n# dynamic block — generate nested blocks from a collection\ndynamic "ingress" {\n  for_each = var.allowed_ports\n  content {\n    from_port = ingress.value\n    to_port   = ingress.value\n    protocol  = "tcp"\n  }\n}</pre></div><div class="gotcha"><div class="gotcha-label">Provisioners = last resort</div>HashiCorp explicitly calls <code>local-exec</code>/<code>remote-exec</code> a last resort — they break the declarative model and aren\'t tracked in state. Prefer user data, cloud-init, AMIs (Packer), or a config-management tool. Reach for provisioners only when no native option exists.</div>'}
  ]
});
