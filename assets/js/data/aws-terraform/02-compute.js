window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Compute',
  number: 'TAB 2',
  title: 'Compute & Containers',
  color: 'var(--accent3)',
  what: 'Where your code runs: <em>EC2 + Auto Scaling, Lambda/serverless, and containers (ECS, EKS, Fargate)</em>. Be ready to justify which compute model fits a given workload.',
  topics:[
    {name:'EC2 — Instances, AMIs, Pricing',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">EC2 ESSENTIALS</span></div><pre>Instance families   General (t/m), Compute (c), Memory (r/x),\n                    Storage (i/d), Accelerated (g/p GPU).\nAMI                 Golden image (OS + baked software). Use for fast,\n                    immutable boot; build with Packer / Image Builder.\nUser data           Bootstrap script run at first boot.\nStorage             EBS (network block, persists) vs instance store (ephemeral).\nPricing             On-Demand | Reserved/Savings Plans (commit, ~up to 72% off)\n                    | Spot (up to ~90% off, can be reclaimed) | Dedicated.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>For stateless, fault-tolerant fleets I mix Spot for cost with On-Demand/Reserved baseline capacity. I bake AMIs so instances boot immutable — no config drift from per-boot provisioning.</div>'},
    {name:'Auto Scaling Groups & Launch Templates',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">ASG</span></div><pre>Launch Template   Defines AMI, type, SG, user data, IAM profile (versioned).\nASG               min / desired / max capacity across multiple AZs.\nScaling policies  Target tracking (e.g. CPU 50%) | step | scheduled |\n                  predictive.\nHealth checks     EC2 + ELB health → replace unhealthy instances.\nLifecycle hooks   Pause on launch/terminate for warm-up / drain.</pre></div><div class="explanation">ASGs give <strong>self-healing</strong> (replace failed instances) and <strong>elasticity</strong> (scale on demand) across AZs. Pair with an ALB target group so new instances register automatically.</div>'},
    {name:'Lambda & Serverless',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">LAMBDA</span></div><pre>Event-driven functions, no servers to manage, pay per ms + requests.\nTriggers      API Gateway, S3, SQS, SNS, EventBridge, DynamoDB Streams.\nLimits        15 min max, /tmp + memory configurable, package/layer size.\nCold start    First invoke initializes runtime; mitigate w/ provisioned\n              concurrency or keeping packages small.\nConcurrency   Scales horizontally automatically; set reserved concurrency\n              to protect downstream systems.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">When would you NOT use Lambda?</div><div class="q-answer">Long-running jobs (&gt;15 min), steady high-throughput workloads where always-on compute is cheaper, very latency-sensitive paths hurt by cold starts, or apps needing specialized hardware/persistent local state. Then prefer ECS/EKS/Fargate or EC2.</div></div>'},
    {name:'Containers — ECS, EKS, Fargate',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CONTAINER OPTIONS</span></div><pre>ECS        AWS-native orchestrator. Simpler, deep AWS integration.\nEKS        Managed Kubernetes. Portable, ecosystem, more complexity.\nFargate    Serverless data plane for ECS/EKS — no nodes to manage,\n           pay per task vCPU/memory.\nEC2 launch Self-managed nodes — more control, bin-packing, GPU, cost\n           tuning, but you patch/scale the cluster.\n\nImages live in ECR. Tasks/pods get IAM via task role / IRSA.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>ECS+Fargate for the fastest path with least ops; EKS when the team already knows Kubernetes or needs portability and the CNCF ecosystem. Fargate removes node management; EC2 launch type wins on cost control and special hardware at scale.</div>'}
  ]
});
