window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Storage & Data',
  number: 'TAB 4',
  title: 'Storage & Databases',
  color: 'var(--accent2)',
  what: 'Where state lives: <em>S3, block/file storage (EBS/EFS), relational (RDS/Aurora), and NoSQL (DynamoDB)</em>. Know how to pick a data store from access pattern and consistency needs.',
  topics:[
    {name:'S3 — Object Storage',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">S3 KEY FACTS</span></div><pre>Storage classes  Standard | Intelligent-Tiering | Standard-IA |\n                 One Zone-IA | Glacier Instant/Flexible/Deep Archive.\nDurability       11 nines (99.999999999%); data replicated across AZs.\nConsistency      Strong read-after-write for all operations.\nSecurity         Block Public Access (on by default), bucket policies,\n                 IAM, SSE-S3 / SSE-KMS encryption, VPC endpoints.\nFeatures         Versioning, Lifecycle rules, Replication (CRR/SRR),\n                 Event notifications, Presigned URLs.</pre></div><div class="gotcha"><div class="gotcha-label">Security</div>Public S3 buckets are a top breach cause. Keep <strong>Block Public Access</strong> on, serve content via CloudFront + OAC, enforce encryption with a bucket policy, and use lifecycle rules to tier cold data to Glacier for cost.</div>'},
    {name:'Block & File Storage — EBS, EFS',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CHOOSE</span></div><pre>EBS    Network block volume for ONE EC2 (gp3/io2/st1). Persists\n       independent of instance; snapshot to S3. Single-AZ (attach in same AZ).\nInstance store  Physical disk on the host — ephemeral, lost on stop. Fast/temp.\nEFS    Managed NFS, shared by MANY instances across AZs. Elastic, pay-per-use.\nFSx    Managed Windows / Lustre / NetApp file systems.\n\nRule: EBS = one instance\'s disk; EFS = shared POSIX file system; S3 = objects.</pre></div>'},
    {name:'Relational — RDS & Aurora',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">RDS / AURORA</span></div><pre>RDS engines    PostgreSQL, MySQL, MariaDB, Oracle, SQL Server.\nMulti-AZ       Synchronous standby in another AZ → automatic failover (HA).\nRead replicas  Async copies for read scaling (can be cross-Region).\nAurora         AWS cloud-native MySQL/PostgreSQL-compatible; storage\n               auto-grows, 6 copies across 3 AZs, faster failover,\n               Serverless v2 autoscaling.\nBackups        Automated snapshots + point-in-time recovery.</pre></div><div class="gotcha"><div class="gotcha-label">HA vs scaling</div>Multi-AZ = <strong>availability</strong> (standby, not readable, auto-failover). Read replicas = <strong>read scaling</strong> (readable, async, manual promote). Interviewers love this distinction — don\'t conflate them.</div>'},
    {name:'NoSQL — DynamoDB',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">DYNAMODB</span></div><pre>Model        Key-value / document. Single-digit ms at any scale.\nKeys         Partition key (hash) [+ optional sort key]. Design around\n             access patterns — pick a high-cardinality partition key to\n             avoid hot partitions.\nIndexes      GSI (different PK, eventually consistent) |\n             LSI (same PK, different sort key, defined at create).\nCapacity     On-demand (spiky/unknown) vs Provisioned (+ auto scaling).\nExtras       DynamoDB Streams, TTL, global tables (multi-Region), DAX cache.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">RDS vs DynamoDB — how do you choose?</div><div class="q-answer">RDS for relational data, complex joins/transactions, ad-hoc queries, and a known schema. DynamoDB for massive scale, predictable key-based access, single-digit-ms latency, and serverless ops — but you must model tables around access patterns up front, since joins/ad-hoc queries aren\'t its strength.</div></div>'}
  ]
});
