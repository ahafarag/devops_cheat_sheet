window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Interview Q&A',
  number: 'TAB 7',
  title: 'Azure DevOps Interview Q&A',
  color: 'var(--accent7)',
  what: 'High-frequency Azure DevOps interview questions grouped by area, with answers phrased the way interviewers want to hear them. Cover the question first, then check against the model answer.',
  topics:[
    {name:'General / Concepts Q&A',tag:'must',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What are the main services in Azure DevOps?</div><div class="q-answer">Five: <strong>Boards</strong> (planning/work items), <strong>Repos</strong> (Git), <strong>Pipelines</strong> (CI/CD), <strong>Artifacts</strong> (package feeds), and <strong>Test Plans</strong> (manual testing). They\'re integrated under an Organization → Project hierarchy for end-to-end traceability.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Azure DevOps vs GitHub — when would you pick which?</div><div class="q-answer">Both Microsoft. Azure DevOps is the enterprise ALM suite with strong Boards + Pipelines + fine-grained permissions, common in regulated enterprises. GitHub centers on repos, Actions, and community/OSS. A frequent hybrid: Azure Boards/Pipelines driving GitHub-hosted repos.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What is the Organization → Project → Repo hierarchy for?</div><div class="q-answer">It\'s the security and isolation boundary. The org is the billing/identity root; a project scopes repos, pipelines, boards, and permissions; objects (repos, pipelines, branches) get their own ACLs. Most companies use one org with a project per product/team.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How does Azure DevOps give end-to-end traceability?</div><div class="q-answer">Work item → branch → commits (referencing #id) → PR (links work items + build validation) → build (lists associated work items) → deployment (environment history shows what shipped where). You can answer "what changed in prod and why" from a single chain.</div></div>'},
    {name:'Pipelines Q&A',tag:'must',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">Explain stages, jobs, and steps.</div><div class="q-answer"><strong>Stage</strong> = a major phase (Build, DeployDev) that can depend on others. <strong>Job</strong> = a set of steps that run on a single agent; jobs run in parallel unless <code>dependsOn</code> is set. <strong>Step</strong> = a task or script. Pipeline → stages → jobs → steps.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do PR builds work for an Azure Repos Git repo?</div><div class="q-answer">Via a <strong>Build Validation branch policy</strong>, not the YAML <code>pr:</code> trigger. The <code>pr:</code> trigger only applies to GitHub/Bitbucket sources. This is the single most common Azure Pipelines gotcha.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Difference between $(var), ${{ }}, and $[ ]?</div><div class="q-answer"><code>$(var)</code> macro — expanded at runtime before a step. <code>${{ }}</code> template expression — expanded at compile time (template parsing), used for conditional structure. <code>$[ ]</code> runtime expression — evaluated at runtime for conditions/dependencies.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Microsoft-hosted vs self-hosted agents?</div><div class="q-answer">Hosted = clean managed VM per run, zero maintenance, public network only, billed per parallel job. Self-hosted = you manage it, persistent (caches/tools), can reach private networks, cost-effective at scale. Choose self-hosted for private access, custom tooling, or large caches.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you reuse pipeline logic across teams?</div><div class="q-answer">YAML <strong>templates</strong> (step/job/stage/variable) stored in a shared repo and referenced with <code>@repoAlias</code>. You can mandate a "required template" check on prod environments so every pipeline inherits standard security scans and approvals.</div></div>'},
    {name:'Repos & Boards Q&A',tag:'likely',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What can branch policies enforce?</div><div class="q-answer">Minimum reviewers (with reset-on-push), linked work items, comment resolution, allowed merge types, <strong>build validation</strong> (PR CI), status checks, and auto-included reviewers by path. They make PRs mandatory and protect the default branch.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What is PR auto-complete?</div><div class="q-answer">You set a PR to complete automatically once all branch policies pass (build green, required votes, work items linked). It removes the manual "click merge" wait while still enforcing every gate.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">Which branching strategy and why?</div><div class="q-answer">Trunk-based / Microsoft Release Flow for CD: short-lived branches keep main releasable, feature flags decouple deploy from release, fewer merge conflicts. GitFlow only when maintaining multiple released versions in parallel.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">PAT vs Entra ID auth — which is safer?</div><div class="q-answer">Entra ID (formerly Azure AD) — central identity, conditional access, no long-lived token to leak. PATs are user-scoped, expiring tokens; if you must use them, scope minimally and set short expiry. Org policy can disable PAT creation.</div></div>'},
    {name:'Releases, Security & Scenarios Q&A',tag:'must',body:'<div class="q-block"><div class="q-label">Question</div><div class="q-text">What is an Environment and why use deployment jobs?</div><div class="q-answer">An Environment is a named target (dev/prod) that adds deployment history, approvals &amp; checks, and resource targeting (K8s/VMs). A <code>deployment</code> job (vs plain <code>job</code>) binds to it and supports strategies like runOnce/rolling/canary. Approvals live on the environment, not the YAML — enforcing separation of duties.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you gate a production deployment?</div><div class="q-answer">Environment approvals (named approvers), branch control (only deploy from main), business-hours window, and automated checks (query Azure Monitor for active alerts, invoke a REST API/Function). Combine with exclusive lock to serialize prod deploys.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">How do you handle secrets in pipelines?</div><div class="q-answer">Use secret variables or, better, <strong>Key Vault-backed variable groups</strong> / the AzureKeyVault task so secrets live in KV (rotation, audit, access policies). Secrets are masked in logs and must be mapped explicitly via <code>env:</code> — they\'re not auto-injected.</div></div><div class="q-block"><div class="q-label">Question</div><div class="q-text">What is Workload Identity Federation and why prefer it?</div><div class="q-answer">OIDC-based trust between an Entra app and an Azure RM service connection — Azure DevOps mints short-lived federated tokens per run, so <strong>no client secret is stored</strong>. It eliminates secret rotation and the "expired SPN secret broke prod" outage class.</div></div><div class="q-block"><div class="q-label">Scenario</div><div class="q-text">Walk me through a commit reaching production.</div><div class="q-answer">Push to a feature branch → PR opened → branch policy runs build validation + requires 2 reviewers + linked work item → merge (squash) to main → CI stage builds, tests, scans, publishes artifact → DeployDev deployment job auto-deploys → DeployProd waits on environment approval + Azure Monitor health check → canary/runOnce deploy via a secretless (WIF) Azure RM connection → environment records exactly what shipped.</div></div><div class="q-block"><div class="q-label">Scenario</div><div class="q-text">A nightly build passes but the deploy intermittently fails pulling a dependency. How do you investigate?</div><div class="q-answer">Check the agent type (hosted clean VM may re-download deps — add <code>Cache@2</code>), verify the Artifacts feed/upstream is reachable from the agent\'s network, inspect the failing task logs and retry behavior, confirm service-connection token validity (favor WIF), and add a health gate/retry so a transient registry blip doesn\'t fail the stage.</div></div>'}
  ]
});
