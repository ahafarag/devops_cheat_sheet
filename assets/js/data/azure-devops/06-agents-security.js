window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Agents & Security',
  number: 'TAB 6',
  title: 'Agents, Service Connections & Security',
  color: 'var(--accent5)',
  what: 'How pipelines reach the outside world safely: <em>agent choices, service connections, the permissions model, and modern secretless auth (workload identity federation)</em>.',
  topics:[
    {name:'Microsoft-hosted vs Self-hosted Agents',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">COMPARE</span></div><pre>                  Microsoft-hosted        Self-hosted\nMaintenance       None (Microsoft)        You patch/scale it\nState             Clean VM each run       Persistent (caches, tools)\nNetwork           Public only             Can reach private/on-prem\nSoftware          Preinstalled image      Whatever you install\nCost              Per parallel job        Your infra + 1 free self-hosted\nScaling           Automatic               You (VMSS / scale set agents / k8s)</pre></div><div class="explanation">Run self-hosted agents as a <strong>VM scale set pool</strong> or in containers/Kubernetes to autoscale. Always isolate agents that touch production secrets and keep their OS patched — a compromised agent can exfiltrate every secret it sees.</div>'},
    {name:'Service Connections',tag:'must',body:'<div class="explanation">A <strong>service connection</strong> is a stored, access-controlled credential that lets a pipeline talk to an external system (Azure, a registry, Kubernetes, GitHub). Defined in <em>Project Settings → Service connections</em>.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">COMMON TYPES</span></div><pre>Azure Resource Manager   Deploy to Azure (service principal or WIF)\nDocker Registry          Push/pull images (ACR, Docker Hub)\nKubernetes               Deploy to a cluster\nGitHub / Bitbucket       Source + PR triggers\nGeneric / SSH            Anything else\n\nGuard with: pipeline permissions (which pipelines may use it) +\n            approvals/checks on the connection itself.</pre></div><div class="gotcha"><div class="gotcha-label">Security</div>Restrict each service connection to specific pipelines (disable "grant access to all pipelines"). A prod Azure connection usable by any pipeline is a lateral-movement risk.</div>'},
    {name:'Workload Identity Federation (secretless)',tag:'likely',body:'<div class="explanation">The modern way to authenticate an Azure RM service connection: <strong>Workload Identity Federation (OIDC)</strong>. Azure DevOps presents a short-lived federated token to Entra ID — <strong>no client secret or certificate is stored</strong>, so nothing can leak or expire mid-night.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">WHY IT MATTERS</span></div><pre>Old way:  service principal + client secret stored in the connection\n          → secret rotation pain, leak risk, sudden expiry outages.\nWIF:      federated credential trust between Entra app + the connection\n          → tokens are minted per run, short-lived, nothing to rotate.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>I migrate Azure RM service connections to workload identity federation to eliminate stored secrets — it removes the rotation burden and the "expired SPN secret broke prod deploys" class of incident entirely.</div>'},
    {name:'Permissions & Security Model',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">SECURITY LAYERS</span></div><pre>Organization → Project → Object (repo / pipeline / area path)\n\nSecurity groups   Project Admins, Contributors, Readers (+ custom).\n                  Prefer Entra ID groups → map to ADO groups.\nObject perms      Per repo/branch/pipeline/area-path ACLs\n                  (Allow / Deny / Not set; Deny wins).\nPATs &amp; tokens     Scoped, expiring. Org policy can restrict creation.\nPipeline security Approvals, branch control, protected resources,\n                  and "settable at queue time" variable controls.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">How do you secure a production deployment pipeline?</div><div class="q-answer">Environment approvals (separation of duties), branch control so only <code>main</code> deploys, a Key Vault-backed secretless (WIF) service connection scoped to that one pipeline, required reviewers + build validation on the branch, and least-privilege Entra groups. Audit via the activity log.</div></div>'}
  ]
});
