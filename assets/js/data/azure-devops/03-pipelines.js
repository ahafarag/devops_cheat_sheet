window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Pipelines',
  number: 'TAB 3',
  title: 'Azure Pipelines (YAML CI)',
  color: 'var(--accent3)',
  what: 'The heart of Azure DevOps: <em>YAML pipelines — stages, jobs, steps, triggers, templates, and agents</em>. Expect to read and write pipeline YAML on the spot.',
  topics:[
    {name:'Pipeline Anatomy — Stages / Jobs / Steps',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">azure-pipelines.yml</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>trigger:\n  branches:\n    include: [ main ]\n\npool:\n  vmImage: ubuntu-latest\n\nvariables:\n  buildConfiguration: Release\n\nstages:\n- stage: Build\n  jobs:\n  - job: Compile\n    steps:\n    - task: DotNetCoreCLI@2\n      inputs:\n        command: build\n        arguments: --configuration $(buildConfiguration)\n    - script: dotnet test\n      displayName: Run tests\n    - publish: $(System.DefaultWorkingDirectory)/out\n      artifact: drop</pre></div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">HIERARCHY</span></div><pre>Pipeline → Stages → Jobs → Steps (tasks/scripts)\n\nStage  unit of deployment/grouping; can depend on other stages\nJob    runs on ONE agent; jobs in a stage run in parallel by default\nStep   a task (e.g. DotNetCoreCLI@2) or a script/bash/pwsh line\n\nStages &amp; jobs run in parallel unless you set dependsOn.</pre></div>'},
    {name:'Triggers — CI, PR, Scheduled',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">TRIGGERS</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># CI trigger — runs on push to these branches/paths\ntrigger:\n  branches:\n    include: [ main, releases/* ]\n    exclude: [ docs/* ]\n  paths:\n    include: [ src/* ]\n\n# PR trigger — GitHub/Bitbucket only (Azure Repos uses branch policy)\npr:\n  branches:\n    include: [ main ]\n\n# Scheduled (cron, UTC)\nschedules:\n- cron: "0 2 * * *"\n  displayName: Nightly build\n  branches:\n    include: [ main ]\n  always: true</pre></div><div class="gotcha"><div class="gotcha-label">Critical gotcha</div>For <strong>Azure Repos Git</strong>, the <code>pr:</code> trigger is ignored — PR builds are configured via a <strong>Build Validation</strong> branch policy. The <code>pr:</code> key only works for GitHub and Bitbucket Cloud sources.</div>'},
    {name:'Templates & Reuse (DRY pipelines)',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">template usage</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># build-steps.yml (a step template)\nparameters:\n  buildConfig: Release\nsteps:\n- script: dotnet build -c ${{ parameters.buildConfig }}\n\n# main pipeline references it\nsteps:\n- template: build-steps.yml\n  parameters:\n    buildConfig: Debug\n\n# Reference a template from ANOTHER repo (governance)\nresources:\n  repositories:\n  - repository: templates\n    type: git\n    name: Shared/pipeline-templates\nstages:\n- template: ci-stage.yml@templates</pre></div><div class="phrase"><div class="phrase-label">Say this</div>I centralize pipeline logic in templates stored in a shared repo so every team inherits the same security scans and approval flow. <code>${{ }}</code> template expressions are evaluated at <strong>compile time</strong>, which is what lets templates conditionally include steps.</div>'},
    {name:'Agents & Pools',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">AGENTS</span></div><pre>Microsoft-hosted   Fresh clean VM per job (ubuntu/windows/macOS images).\n                   No maintenance; ~10GB workspace; job time limits;\n                   parallel jobs are billed.\nSelf-hosted        You install the agent on your VM/container/k8s.\n                   Persistent — caches, custom tools, private network\n                   access (e.g. on-prem DB, private endpoints).\n\nPools group agents. Demands/capabilities match jobs to agents.\nUse self-hosted when you need private network access, custom\nsoftware, or to avoid per-minute hosted costs at scale.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">When would you choose a self-hosted agent?</div><div class="q-answer">When the build needs access to a private network/resources (on-prem DB, private endpoints), specialized or licensed software pre-installed, large persistent caches for speed, or to control cost at high build volume. Trade-off: you own patching, scaling, and security of the agent host.</div></div>'},
    {name:'Tasks, Scripts & Marketplace',tag:'possible',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">STEP TYPES</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>steps:\n- script: echo hello              # cmd/bash depending on agent OS\n- bash: ./build.sh\n- pwsh: Write-Host "PowerShell Core"\n- task: AzureCLI@2               # marketplace/built-in tasks: name@major\n  inputs:\n    azureSubscription: my-service-connection\n    scriptType: bash\n    scriptLocation: inlineScript\n    inlineScript: az group list</pre></div><div class="explanation">Tasks are versioned (<code>Task@Major</code>) so a major bump can\'t silently break you. Beyond built-ins, the <strong>Visual Studio Marketplace</strong> provides community tasks (SonarQube, Terraform, etc.) — vet third-party tasks before trusting them with secrets.</div>'}
  ]
});
