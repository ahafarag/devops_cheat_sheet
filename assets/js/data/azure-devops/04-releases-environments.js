window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Releases & Envs',
  number: 'TAB 4',
  title: 'Releases, Environments & Deployment',
  color: 'var(--accent2)',
  what: 'Shipping safely: <em>multi-stage YAML deployments, Environments, approvals &amp; checks, and deployment strategies</em> (runOnce, rolling, canary) — plus how classic Release pipelines differ.',
  topics:[
    {name:'Multi-Stage Deployment Pipelines',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">CI then CD with a deployment job</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>stages:\n- stage: Build\n  jobs:\n  - job: build\n    steps: [ { script: dotnet publish -o $(Build.ArtifactStagingDirectory) }, { publish: $(Build.ArtifactStagingDirectory), artifact: drop } ]\n\n- stage: DeployDev\n  dependsOn: Build\n  jobs:\n  - deployment: deployDev      # a DEPLOYMENT job, not a plain job\n    environment: dev           # binds to an Environment (gives history + checks)\n    strategy:\n      runOnce:\n        deploy:\n          steps:\n          - download: current\n            artifact: drop\n          - task: AzureWebApp@1\n            inputs:\n              azureSubscription: prod-conn\n              appName: my-app-dev</pre></div><div class="explanation">A <code>deployment</code> job (vs a normal <code>job</code>) targets an <strong>environment</strong>, records deployment history, and supports strategies. Stages chain with <code>dependsOn</code> and can be gated with conditions.</div>'},
    {name:'Environments & Resources',tag:'must',body:'<div class="explanation">An <strong>Environment</strong> is a named target (dev/test/prod) that adds <strong>deployment history</strong>, <strong>approvals &amp; checks</strong>, and can hold <strong>resources</strong> (Kubernetes namespaces, VMs) for per-resource targeting. Environments exist only for YAML pipelines.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">WHAT ENVIRONMENTS GIVE YOU</span></div><pre>- Traceability: which commit/PR/work items deployed where, and when\n- Security: approvals &amp; checks attached to the environment (not the YAML)\n- Targeting: deploy to specific VM/K8s resources registered in the env\n- Permissions: who can deploy to prod vs dev</pre></div>'},
    {name:'Approvals & Checks (gating prod)',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">CHECKS ON AN ENVIRONMENT</span></div><pre>Approvals          Named approvers must sign off before deploy.\nBranch control     Only deploy from allowed branches (e.g. main).\nBusiness hours     Defer deploy to an allowed time window.\nInvoke REST API    Call an external service; pass/fail on response.\nInvoke Azure Function\nQuery Azure Monitor  Block if active alerts (health gate).\nExclusive lock     Serialize deployments to the same environment.\nRequired template  Force the pipeline to extend an approved template.</pre></div><div class="phrase"><div class="phrase-label">Say this</div>Approvals live on the <strong>environment</strong>, not in the YAML — so a developer editing the pipeline can\'t remove the prod approval. That separation of duties is exactly why environments matter for compliance.</div>'},
    {name:'Deployment Strategies',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang yaml">STRATEGIES</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>strategy:\n  runOnce:        # simplest: preDeploy → deploy → routeTraffic → postDeploy → on: {failure,success}\n    deploy: { steps: [ ... ] }\n\n  rolling:        # update N targets at a time (VM resources)\n    maxParallel: 2\n    deploy: { steps: [ ... ] }\n\n  canary:         # ship to a small % first, then increase\n    increments: [10, 20, 50]\n    deploy: { steps: [ ... ] }</pre></div><div class="explanation">Each strategy exposes lifecycle hooks (<code>preDeploy</code>, <code>deploy</code>, <code>routeTraffic</code>, <code>postDeploy</code>, and <code>on: failure/success</code>) so you can health-check and roll back. Blue/green is achieved with slots (App Service deployment slots) + <code>routeTraffic</code>.</div>'},
    {name:'Classic Release Pipelines',tag:'possible',body:'<div class="explanation">The older <strong>UI-based Releases</strong> hub (Pipelines → Releases) predates YAML CD. It has visual stages, pre/post-deploy approvals and gates, and artifact triggers.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">YAML vs CLASSIC</span></div><pre>Classic Release   Drag-drop UI, not version-controlled with the code.\nYAML multi-stage  Pipeline-as-code, lives with the repo, reviewable in PRs.\n\nMicrosoft now recommends YAML multi-stage pipelines for new work.\nKnow classic exists (you\'ll meet it in legacy projects) but prefer YAML.</pre></div>'}
  ]
});
