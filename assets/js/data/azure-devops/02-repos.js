window.AREAS = window.AREAS || [];
window.AREAS.push({
  nav: 'Repos',
  number: 'TAB 2',
  title: 'Azure Repos — Git, Policies & PRs',
  color: 'var(--accent1)',
  what: 'Source control in Azure DevOps: <em>Git repositories, branch policies that gate the default branch, pull requests, and the branching strategy</em> you should be able to defend.',
  topics:[
    {name:'Git in Azure Repos',tag:'must',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">CLONE &amp; AUTH</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre># HTTPS clone (PAT or Git Credential Manager)\ngit clone https://org@dev.azure.com/org/Project/_git/Repo\n\n# az repos CLI\naz repos list --project Project\naz repos create --name NewRepo --project Project</pre></div><div class="explanation">Azure Repos supports unlimited private <strong>Git</strong> repos (and legacy TFVC). Auth via <strong>Personal Access Tokens (PAT)</strong>, SSH keys, or Microsoft Entra ID. PATs are scoped and expiring — prefer short-lived tokens and Entra over long-lived PATs.</div><div class="gotcha"><div class="gotcha-label">Gotcha</div>A PAT is tied to a user and inherits that user\'s permissions — leaking one is a credential breach. Scope PATs minimally (e.g. <code>Code: Read</code>), set short expiry, and store in a secret manager, never in a repo.</div>'},
    {name:'Branch Policies (the gate)',tag:'must',body:'<div class="explanation">Branch policies protect the default/release branches — they make PRs <strong>mandatory</strong> and enforce quality before merge. This is the #1 Azure Repos interview topic.</div><div class="code-wrap"><div class="code-label"><span class="code-lang concept">POLICY OPTIONS</span></div><pre>Require a minimum number of reviewers   (e.g. 2; reset on new push)\nCheck for linked work items             (traceability)\nCheck for comment resolution            (no unresolved threads)\nLimit merge types                       (squash / rebase / no-FF)\nBuild validation                        (a pipeline must pass — this is\n                                         how CI runs on PRs for Azure Repos)\nStatus checks                           (external services post status)\nAutomatically included reviewers        (e.g. CODEOWNERS-style by path)\nRequire approval from additional groups</pre></div><div class="phrase"><div class="phrase-label">Say this</div>For Azure Repos, PR CI is wired through <strong>Build Validation</strong> branch policy — not a <code>pr:</code> trigger in YAML. The <code>pr:</code> trigger only works for GitHub/Bitbucket repos. That distinction trips people up constantly.</div>'},
    {name:'Pull Requests & Code Review',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang bash">az repos pr</span><button class="copy-btn" onclick="copyCode(this)">Copy</button></div><pre>az repos pr create --source-branch feature/x --target-branch main \\\n  --title "Add X" --work-items 123 456\naz repos pr list --status active\naz repos pr set-vote --id 42 --vote approve   # approve | reject | wait-for-author</pre></div><div class="explanation">PR features: required/optional reviewers, vote states (Approve / Approve-with-suggestions / Wait-for-author / Reject), comment threads, <strong>auto-complete</strong> (merges automatically once all policies pass), and draft PRs. Merge strategies: merge commit, squash, rebase, semi-linear.</div>'},
    {name:'Branching Strategies',tag:'likely',body:'<div class="code-wrap"><div class="code-label"><span class="code-lang concept">STRATEGIES</span></div><pre>Trunk-based      Short-lived feature branches → frequent merges to main.\n                 Pairs with feature flags + CI. Microsoft\'s recommendation.\nRelease Flow     Microsoft\'s own: trunk-based + short release branches\n                 cut per deployment, hotfix cherry-picked back.\nGitFlow          develop + release + hotfix branches. Heavier; good for\n                 versioned products with scheduled releases.\nGitHub Flow      main + feature branches + PR. Simple, continuous deploy.</pre></div><div class="q-block"><div class="q-label">Likely Question</div><div class="q-text">Which branching strategy do you recommend and why?</div><div class="q-answer">Trunk-based / Release Flow for continuous delivery: short-lived branches reduce merge hell and keep main always releasable, with feature flags decoupling deploy from release. GitFlow only when you maintain multiple released versions in parallel.</div></div>'}
  ]
});
